# Code generated by `cargo soothfast bind gen`. DO NOT EDIT.

[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "finance-query"
version = "3.1.0"
requires-python = ">=3.9"
description = "finance-query, the Rust market-data library, called in-process from Python"

[tool.maturin]
module-name = "finance_query"
features = ["pyo3/extension-module"]
