// Code generated by `cargo soothfast sdk gen`. DO NOT EDIT.
/** Client for finance-query. */

import type * as models from "./models.js";
import { AsyncPager, Transport, pathSeg, queryOf, type BaseUrl, type ClientOptions } from "./runtime.js";
import { embeddedBaseUrl, type EmbedConfig } from "./server.js";

/** The hosted deployment. Pass it explicitly to talk to that instead of the bundled server. */
export const DEFAULT_BASE_URL = "https://finance-query.com";

/**
 * The server this package bundles.
 *
 * Set `FINANCE_QUERY_BASE_URL` to talk to an already-running instance instead of spawning one, or `FINANCE_QUERY_SERVER_BIN` to point at a different binary.
 */
export const EMBED: EmbedConfig = {
  binary: "finance-query-server",
  args: [],
  baseUrlEnv: "FINANCE_QUERY_BASE_URL",
  binEnv: "FINANCE_QUERY_SERVER_BIN",
  packagePrefix: "finance-query",
  env: {
    "HOST": "127.0.0.1",
    "PORT": "0",
  },
};

/**
 * Environment for the bundled server.
 *
 * Every key is optional, and the names are the server's own — the same ones a deployment sets.
 */
export interface ServerEnv {
  /**
   * Server Configuration
   *
   * Defaults to `8000`.
   */
  PORT?: string;
  /**
   * Bind address. 0.0.0.0 serves every interface; an SDK-spawned server sets
   *
   * 127.0.0.1 so nothing off the machine can reach it
   *
   * Defaults to `0.0.0.0`.
   */
  HOST?: string;
  /**
   * Logging
   *
   * Options: DEBUG, INFO, WARNING, ERROR
   *
   * Defaults to `INFO`.
   */
  LOG_LEVEL?: string;
  /**
   * Options: json, text
   *
   * Defaults to `json`.
   */
  LOG_FORMAT?: string;
  /**
   * Redis Cache (optional)
   *
   * Unset unless you set it.
   */
  REDIS_URL?: string;
  /**
   * Rate Limiting
   *
   * Requests per minute, applied independently per client IP (default: 600 = 10 req/sec)
   *
   * Defaults to `600`.
   */
  RATE_LIMIT_PER_MINUTE?: string;
  /**
   * API Defaults
   *
   * Options: 1m, 5m, 15m, 30m, 1h, 1d, 1wk, 1mo, 3mo
   *
   * Defaults to `1d`.
   */
  DEFAULT_INTERVAL?: string;
  /**
   * Options: 1d, 5d, 1mo, 3mo, 6mo, 1y, 2y, 5y, 10y, ytd, max
   *
   * Defaults to `1mo`.
   */
  DEFAULT_RANGE?: string;
  /**
   * Default Query Limits
   *
   * Defaults to `5`.
   */
  RECOMMENDATIONS_LIMIT?: string;
  /** Defaults to `10`. */
  SEARCH_HITS?: string;
  /** Defaults to `25`. */
  SCREENERS_COUNT?: string;
  /**
   * SEC EDGAR Integration (optional)
   *
   * Contact email required by SEC - included in User-Agent header
   *
   * Leave commented out to disable EDGAR endpoints
   *
   * Unset unless you set it.
   */
  EDGAR_EMAIL?: string;
  /**
   * FRED API Integration (optional)
   *
   * Free API key from https://fred.stlouisfed.org/docs/api/api_key.html
   *
   * Leave commented out to disable /v2/fred/* endpoints
   *
   * Note: If running a public server, your terms of use must state that users are bound
   *
   * by the FRED API Terms of Use: https://fred.stlouisfed.org/docs/api/terms_of_use.html
   *
   * Unset unless you set it.
   */
  FRED_API_KEY?: string;
  /**
   * Polygon.io - https://polygon.io/dashboard/api-keys
   *
   * Unset unless you set it.
   */
  POLYGON_API_KEY?: string;
  /**
   * Financial Modeling Prep - https://site.financialmodelingprep.com/developer/docs
   *
   * Unset unless you set it.
   */
  FMP_API_KEY?: string;
  /**
   * Alpha Vantage - https://www.alphavantage.co/support/#api-key
   *
   * Unset unless you set it.
   */
  ALPHAVANTAGE_API_KEY?: string;
  /**
   * US Bureau of Labor Statistics - https://data.bls.gov/registrationEngine/
   *
   * Raises the request quota; the BLS API also works unregistered
   *
   * Unset unless you set it.
   */
  BLS_API_KEY?: string;
  /**
   * OpenFIGI - https://www.openfigi.com/api
   *
   * Raises the rate limit on symbol lookups; works unregistered
   *
   * Unset unless you set it.
   */
  OPENFIGI_API_KEY?: string;

  [key: string]: string | undefined;
}

/** Client options, plus the environment for the bundled server. */
export interface EmbeddedClientOptions extends ClientOptions {
  /** Applied when this client starts the bundled server. Ignored when a base URL is passed, or when the environment already points at a running instance — there is no server of ours to configure. */
  serverEnv?: ServerEnv;
}

/** Query parameters for {@link Client.getAnalysis}. */
export interface GetAnalysisOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
}

/** Query parameters for {@link Client.getBatchCapitalGains}. */
export interface GetBatchCapitalGainsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Time range (default: max) */
  range?: models.TimeRange;
  /** Comma-separated symbols (required) */
  symbols: string;
}

/** Query parameters for {@link Client.getBatchCharts}. */
export interface GetBatchChartsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Candle interval (default: 1d) */
  interval?: models.Interval;
  /** Detect candlestick patterns and include per-candle signals in response. */
  patterns?: boolean;
  /** Time range (default: 1mo) */
  range?: models.TimeRange;
  /** Comma-separated symbols (required) */
  symbols: string;
}

/** Query parameters for {@link Client.iterBatchCharts}. */
export interface GetBatchChartsIterOptions extends GetBatchChartsOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getBatchDividends}. */
export interface GetBatchDividendsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Time range (default: max) */
  range?: models.TimeRange;
  /** Comma-separated symbols (required) */
  symbols: string;
}

/** Query parameters for {@link Client.iterBatchDividends}. */
export interface GetBatchDividendsIterOptions extends GetBatchDividendsOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getBatchFinancials}. */
export interface GetBatchFinancialsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Frequency: annual or quarterly (default: annual) */
  frequency?: models.Frequency;
  /** Comma-separated list of metric names to include in the statement (e.g. "TotalRevenue,NetIncome") */
  metrics?: string;
  /** Statement type (required): income, balance, cashflow */
  statement: models.StatementType;
  /** Comma-separated symbols (required) */
  symbols: string;
}

/** Query parameters for {@link Client.getBatchIndicators}. */
export interface GetBatchIndicatorsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Candle interval (default: 1d) */
  interval?: models.Interval;
  /** Time range (default: 1mo) */
  range?: models.TimeRange;
  /** Comma-separated symbols (required) */
  symbols: string;
}

/** Query parameters for {@link Client.iterBatchIndicators}. */
export interface GetBatchIndicatorsIterOptions extends GetBatchIndicatorsOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getBatchOptions}. */
export interface GetBatchOptionsOptions {
  /** Expiration date (Unix timestamp, optional) */
  date?: number;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Comma-separated symbols (required) */
  symbols: string;
}

/** Query parameters for {@link Client.iterBatchOptions}. */
export interface GetBatchOptionsIterOptions extends GetBatchOptionsOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getBatchRecommendations}. */
export interface GetBatchRecommendationsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Maximum number of similar symbols per requested symbol */
  limit?: number;
  /** Opaque continuation cursor from a previous response's `pageInfo.endCursor` */
  pageCursor?: string;
  /** Max symbols per page; omitted (with page_cursor also omitted) = every requested symbol's recommendations as a bare array, unchanged from pre-pagination behavior */
  pageLimit?: number;
  /** Comma-separated symbols (required) */
  symbols: string;
}

/** Query parameters for {@link Client.getBatchSplits}. */
export interface GetBatchSplitsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Time range (default: max) */
  range?: models.TimeRange;
  /** Comma-separated symbols (required) */
  symbols: string;
}

/** Query parameters for {@link Client.getCalendar}. */
export interface GetCalendarOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Forward window: 1wk/5d, 1mo, 3mo, etc. (default: 1mo) */
  range?: models.TimeRange;
  /** Comma-separated symbols (required) */
  symbols: string;
}

/** Query parameters for {@link Client.getCapitalGains}. */
export interface GetCapitalGainsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Time range (default: max) */
  range?: models.TimeRange;
}

/** Query parameters for {@link Client.getChart}. */
export interface GetChartOptions {
  /** End date as Unix timestamp (seconds). Defaults to now when `start` is set. */
  end?: number;
  /** Include events (dividends, splits, capital gains) in response */
  events?: boolean;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Candle interval (e.g. 1m, 5m, 1h, 1d; default: 1d) */
  interval?: models.Interval;
  /** Detect candlestick patterns and include per-candle signals in response. The `patterns` array aligns 1:1 with the `candles` array; `null` means no pattern was detected on that bar. */
  patterns?: boolean;
  /** Time range (e.g. 1d, 5d, 1mo, 1y, max; default: 1mo) */
  range?: models.TimeRange;
  /** Start date as Unix timestamp (seconds). When provided together with `end`, overrides `range` and uses absolute date boundaries. */
  start?: number;
}

/** Query parameters for {@link Client.iterChart}. */
export interface GetChartIterOptions extends GetChartOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getCommitmentsOfTraders}. */
export interface GetCommitmentsOfTradersOptions {
  /** Opaque continuation cursor from a previous response's `pageInfo.endCursor` */
  cursor?: string;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Max weekly observations per page; omitted (with cursor also omitted) = every observation as a bare array */
  limit?: number;
}

/** Query parameters for {@link Client.getCompanyProfile}. */
export interface GetCompanyProfileOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
}

/** Query parameters for {@link Client.getCongressionalTrades}. */
export interface GetCongressionalTradesOptions {
  /** Opaque continuation cursor from a previous response's `pageInfo.endCursor` */
  cursor?: string;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Max entries per page; omitted (with cursor also omitted) = every fetched entry as a bare array, unchanged from pre-pagination behavior */
  limit?: number;
}

/** Query parameters for {@link Client.getCryptoCoin}. */
export interface GetCryptoCoinOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Currency to compare against (default: "usd") */
  vsCurrency?: string;
}

/** Query parameters for {@link Client.getCryptoCoins}. */
export interface GetCryptoCoinsOptions {
  /** Number of coins to return (default: 50) */
  count?: number;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Currency to compare against (default: "usd") */
  vsCurrency?: string;
}

/** Query parameters for {@link Client.iterCryptoCoins}. */
export interface GetCryptoCoinsIterOptions extends GetCryptoCoinsOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getCryptoGlobal}. */
export interface GetCryptoGlobalOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
}

/** Query parameters for {@link Client.getCryptoNews}. */
export interface GetCryptoNewsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Maximum number of articles to fetch (default: 20) */
  limit?: number;
  /** Opaque continuation cursor from a previous response's `pageInfo.endCursor` */
  pageCursor?: string;
  /** Max articles per page; omitted (with pageCursor also omitted) = every fetched article (up to `limit`) as a bare array, unchanged from pre-pagination behavior */
  pageLimit?: number;
}

/** Query parameters for {@link Client.getCryptoSearch}. */
export interface GetCryptoSearchOptions {
  /** Opaque continuation cursor from a previous response's `pageInfo.endCursor` */
  cursor?: string;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Max matches per page; omitted (with cursor also omitted) = bare array */
  limit?: number;
  /** Max matches to fetch (default: 25) */
  limitResults?: number;
  /** Free-text query (coin name, symbol, or CoinGecko id) */
  query: string;
}

/** Query parameters for {@link Client.getCryptoTrending}. */
export interface GetCryptoTrendingOptions {
  /** Opaque continuation cursor from a previous response's `pageInfo.endCursor` */
  cursor?: string;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Max coins per page; omitted (with cursor also omitted) = bare array */
  limit?: number;
}

/** Query parameters for {@link Client.getCurrencies}. */
export interface GetCurrenciesOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
}

/** Query parameters for {@link Client.getDividends}. */
export interface GetDividendsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Time range (default: max) */
  range?: models.TimeRange;
}

/** Query parameters for {@link Client.iterDividends}. */
export interface GetDividendsIterOptions extends GetDividendsOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getEarningsSurprises}. */
export interface GetEarningsSurprisesOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
}

/** Query parameters for {@link Client.getEarningsTranscript}. */
export interface GetEarningsTranscriptOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Fiscal quarter (Q1, Q2, Q3, Q4). Required alongside `year` for providers with no "latest" shortcut (Alpha Vantage); Yahoo defaults to latest when omitted. */
  quarter?: models.Quarter;
  /** Fiscal year. See `quarter`. */
  year?: number;
}

/** Query parameters for {@link Client.getEdgarCik}. */
export interface GetEdgarCikOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
}

/** Query parameters for {@link Client.getEdgarFacts}. */
export interface GetEdgarFactsOptions {
  /** Comma-separated XBRL concept names to filter to; omitted = curated defaults */
  concepts?: string;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** XBRL taxonomy (default: "us-gaap"); also try "ifrs-full" or "dei" */
  taxonomy?: string;
}

/** Query parameters for {@link Client.iterEdgarFacts}. */
export interface GetEdgarFactsIterOptions extends GetEdgarFactsOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getEdgarSearch}. */
export interface GetEdgarSearchOptions {
  /** End date in YYYY-MM-DD format */
  endDate?: string;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Comma-separated form types (e.g., "10-K,10-Q") */
  forms?: string;
  /** Pagination offset (default: 0) */
  from?: number;
  /** Search query string (required) */
  q: string;
  /** Page size (default: 100, max: 100) */
  size?: number;
  /** Start date in YYYY-MM-DD format */
  startDate?: string;
}

/** Query parameters for {@link Client.getEdgarSubmissions}. */
export interface GetEdgarSubmissionsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
}

/** Query parameters for {@link Client.iterEdgarSubmissions}. */
export interface GetEdgarSubmissionsIterOptions extends GetEdgarSubmissionsOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getExchanges}. */
export interface GetExchangesOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
}

/** Query parameters for {@link Client.getFailsToDeliver}. */
export interface GetFailsToDeliverOptions {
  /** Opaque continuation cursor from a previous response's `pageInfo.endCursor` */
  cursor?: string;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Max entries per page; omitted (with cursor also omitted) = every fetched entry as a bare array, unchanged from pre-pagination behavior */
  limit?: number;
}

/** Query parameters for {@link Client.getFearAndGreed}. */
export interface GetFearAndGreedOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
}

/** Query parameters for {@link Client.getFeeds}. */
export interface GetFeedsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** SEC form type for sec-filings source (e.g., "10-K", "8-K", default: "10-K") */
  formType?: string;
  /** Comma-separated source slugs (default: federal-reserve, sec, marketwatch, bloomberg) */
  sources?: string;
}

/** Query parameters for {@link Client.iterFeeds}. */
export interface GetFeedsIterOptions extends GetFeedsOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getFinancials}. */
export interface GetFinancialsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Frequency: annual or quarterly (default: annual) */
  frequency?: models.Frequency;
  /** Comma-separated list of metric names to include in the statement (e.g. "TotalRevenue,NetIncome") */
  metrics?: string;
}

/** Query parameters for {@link Client.getForexNews}. */
export interface GetForexNewsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Maximum number of articles to fetch (default: 20) */
  limit?: number;
  /** Opaque continuation cursor from a previous response's `pageInfo.endCursor` */
  pageCursor?: string;
  /** Max articles per page; omitted (with pageCursor also omitted) = every fetched article (up to `limit`) as a bare array, unchanged from pre-pagination behavior */
  pageLimit?: number;
}

/** Query parameters for {@link Client.getFredSeries}. */
export interface GetFredSeriesOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
}

/** Query parameters for {@link Client.iterFredSeries}. */
export interface GetFredSeriesIterOptions extends GetFredSeriesOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getFredTreasuryYields}. */
export interface GetFredTreasuryYieldsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Calendar year (default: current year) */
  year?: number;
}

/** Query parameters for {@link Client.iterFredTreasuryYields}. */
export interface GetFredTreasuryYieldsIterOptions extends GetFredTreasuryYieldsOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getGdeltNews}. */
export interface GetGdeltNewsOptions {
  /** Opaque continuation cursor from a previous response's `pageInfo.endCursor` */
  cursor?: string;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Max articles per page; omitted (with cursor also omitted) = bare array */
  limit?: number;
}

/** Query parameters for {@link Client.getGeneralNews}. */
export interface GetGeneralNewsOptions {
  /** Maximum number of articles to return (default: 10) */
  count?: number;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Target language for translated text fields (BCP 47, e.g. "ja", "zh-Hant"); falls back to the Accept-Language header */
  lang?: string;
}

/** Query parameters for {@link Client.iterGeneralNews}. */
export interface GetGeneralNewsIterOptions extends GetGeneralNewsOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getHolders}. */
export interface GetHoldersOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
}

/** Query parameters for {@link Client.iterHolders}. */
export interface GetHoldersIterOptions extends GetHoldersOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getHours}. */
export interface GetHoursOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Region code (e.g., "US", "JP", "GB"). Defaults to US if not specified. */
  region?: models.Region;
}

/** Query parameters for {@link Client.getIndicators}. */
export interface GetIndicatorsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Candle interval (default: 1d) */
  interval?: models.Interval;
  /** Time range (default: 1mo) */
  range?: models.TimeRange;
}

/** Query parameters for {@link Client.getIndices}. */
export interface GetIndicesOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Value format: raw, pretty, or both (default: raw) */
  format?: models.ValueFormat;
  /** Region filter: americas, europe, asia-pacific, middle-east-africa, currencies */
  region?: models.indices_Region;
}

/** Query parameters for {@link Client.getIndustry}. */
export interface GetIndustryOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Value format: raw, pretty, or both (default: raw) */
  format?: models.ValueFormat;
  /** Target language for translated text fields (BCP 47, e.g. "ja", "zh-Hant"); falls back to the Accept-Language header */
  lang?: string;
}

/** Query parameters for {@link Client.getMarketSummary}. */
export interface GetMarketSummaryOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Value format: raw, pretty, or both (default: raw) */
  format?: models.ValueFormat;
  /** Target language for translated text fields (BCP 47, e.g. "ja", "zh-Hant"); falls back to the Accept-Language header */
  lang?: string;
  /** Region code for localization (e.g., "US", "JP", "GB") */
  region?: models.Region;
}

/** Query parameters for {@link Client.getNews}. */
export interface GetNewsOptions {
  /** Maximum number of articles to return (default: 10) */
  count?: number;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Target language for translated text fields (BCP 47, e.g. "ja", "zh-Hant"); falls back to the Accept-Language header */
  lang?: string;
}

/** Query parameters for {@link Client.iterNews}. */
export interface GetNewsIterOptions extends GetNewsOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getOptions}. */
export interface GetOptionsOptions {
  /** Optional expiration date (Unix timestamp) */
  date?: number;
  /** Comma-separated list of fields to include in response */
  fields?: string;
}

/** Query parameters for {@link Client.iterOptions}. */
export interface GetOptionsIterOptions extends GetOptionsOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getQuote}. */
export interface GetQuoteOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Value format: raw, pretty, or both (default: raw) */
  format?: models.ValueFormat;
  /** Target language for translated text fields (BCP 47, e.g. "ja", "zh-Hant"); falls back to the Accept-Language header */
  lang?: string;
  /** Whether to include company logo URL (default: false) */
  logo?: boolean;
}

/** Query parameters for {@link Client.getQuoteType}. */
export interface GetQuoteTypeOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
}

/** Query parameters for {@link Client.getQuotes}. */
export interface GetQuotesOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Value format: raw, pretty, or both (default: raw) */
  format?: models.ValueFormat;
  /** Target language for translated text fields (BCP 47, e.g. "ja", "zh-Hant"); falls back to the Accept-Language header */
  lang?: string;
  /** Whether to include company logo URLs (default: false) */
  logo?: boolean;
  /** Comma-separated symbols (required) */
  symbols: string;
}

/** Query parameters for {@link Client.iterQuotes}. */
export interface GetQuotesIterOptions extends GetQuotesOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Query parameters for {@link Client.getRecommendations}. */
export interface GetRecommendationsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Maximum number of similar symbols to return */
  limit?: number;
}

/** Query parameters for {@link Client.getRisk}. */
export interface GetRiskOptions {
  /** Optional benchmark symbol for beta calculation (e.g., "SPY") */
  benchmark?: string;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Candle interval (default: 1d) */
  interval?: models.Interval;
  /** Time range (default: 1mo) */
  range?: models.TimeRange;
}

/** Query parameters for {@link Client.getScreeners}. */
export interface GetScreenersOptions {
  /** Number of results (default: 25, max: 250) */
  count?: number;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Value format: raw, pretty, or both (default: raw) */
  format?: models.ValueFormat;
}

/** Query parameters for {@link Client.getSector}. */
export interface GetSectorOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Value format: raw, pretty, or both (default: raw) */
  format?: models.ValueFormat;
  /** Target language for translated text fields (BCP 47, e.g. "ja", "zh-Hant"); falls back to the Accept-Language header */
  lang?: string;
}

/** Query parameters for {@link Client.getSpark}. */
export interface GetSparkOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Candle interval (default: 1d) */
  interval?: models.Interval;
  /** Time range (default: 1mo) */
  range?: models.TimeRange;
  /** Comma-separated symbols (required) */
  symbols: string;
}

/** Query parameters for {@link Client.getSplits}. */
export interface GetSplitsOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Time range (default: max) */
  range?: models.TimeRange;
}

/** Query parameters for {@link Client.getTranscript}. */
export interface GetTranscriptOptions {
  /** Target language for translated text fields (BCP 47, e.g. "ja", "zh-Hant"); falls back to the Accept-Language header */
  lang?: string;
  /** Fiscal quarter (Q1, Q2, Q3, Q4). If not provided, returns latest. */
  quarter?: models.Quarter;
  /** Fiscal year. If not provided with quarter, returns latest. */
  year?: number;
}

/** Query parameters for {@link Client.getTranscripts}. */
export interface GetTranscriptsOptions {
  /** Target language for translated text fields (BCP 47, e.g. "ja", "zh-Hant"); falls back to the Accept-Language header */
  lang?: string;
  /** Maximum number of transcripts to return. If not provided, returns all. */
  limit?: number;
}

/** Query parameters for {@link Client.getTrending}. */
export interface GetTrendingOptions {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Region code for localization (e.g., "US", "JP", "GB") */
  region?: models.Region;
}

/** Query parameters for {@link Client.lookup}. */
export interface LookupOptions {
  /** Maximum number of results (default: 25) */
  count?: number;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Target language for translated text fields (BCP 47, e.g. "ja", "zh-Hant"); falls back to the Accept-Language header */
  lang?: string;
  /** Include logo URLs (requires additional API call, default: false) */
  logo?: boolean;
  /** Lookup query string (required) */
  q: string;
  /** Region code for lang/region settings (e.g., "US", "JP", "GB") */
  region?: models.Region;
  /** Asset type filter: all, equity, mutualfund, etf, index, future, currency, cryptocurrency */
  type?: models.LookupType;
}

/** Query parameters for {@link Client.search}. */
export interface SearchOptions {
  /** Enable cultural assets/NFT indices (default: false) */
  cultural?: boolean;
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Enable fuzzy matching for typos (default: false) */
  fuzzy?: boolean;
  /** Target language for translated text fields (BCP 47, e.g. "ja", "zh-Hant"); falls back to the Accept-Language header */
  lang?: string;
  /** Enable logo URLs in results (default: true) */
  logo?: boolean;
  /** Maximum number of news results (default: 0 = disabled) */
  news?: number;
  /** Search query string (required) */
  q: string;
  /** Maximum number of quote results (default: 6) */
  quotes?: number;
  /** Region code for lang/region settings (e.g., "US", "JP", "GB") */
  region?: models.Region;
  /** Enable research reports (default: false) */
  research?: boolean;
}

/** Query parameters for {@link Client.iterSearch}. */
export interface SearchIterOptions extends SearchOptions {
  /** Items requested per page. Defaults to 50. */
  limit?: number;
}

/** Generated client for finance-query. */
export class Client {
  private readonly transport: Transport;

  /**
   * Constructed with no base URL, the client starts the bundled server on its first request and talks to that — nothing to deploy, nothing to configure.
   *
   * Pass `DEFAULT_BASE_URL` to use the hosted deployment instead.
   *
   * `options.serverEnv` configures that server — see `ServerEnv` for the knobs it reads.
   */
  constructor(baseUrl?: BaseUrl, options: EmbeddedClientOptions = {}) {
    const target = baseUrl ?? embeddedBaseUrl(EMBED, options.serverEnv);
    this.transport = options.transport ?? new Transport(target, options);
  }

  /** Get analysis data */
  getAnalysis(symbol: string, type: models.AnalysisType, options: GetAnalysisOptions = {}): Promise<models.AnalysisResponse> {
    return this.transport.request<models.AnalysisResponse>("GET", `/v2/analysis/${pathSeg(symbol)}/${pathSeg(type)}`, {
      query: queryOf(options),
    });
  }

  /** Get batch capital gains history */
  getBatchCapitalGains(options: GetBatchCapitalGainsOptions): Promise<models.GqlCapitalGainsBatch> {
    return this.transport.request<models.GqlCapitalGainsBatch>("GET", "/v2/capital-gains", {
      query: queryOf(options),
    });
  }

  /** Get batch historical chart data */
  getBatchCharts(options: GetBatchChartsOptions): Promise<models.GqlChartsBatch> {
    return this.transport.request<models.GqlChartsBatch>("GET", "/v2/charts", {
      query: queryOf(options),
    });
  }

  /**
   * Get batch historical chart data
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterBatchCharts(options: GetBatchChartsIterOptions): AsyncPager<unknown> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<unknown>((cursor) =>
      this.transport.request("GET", "/v2/charts", {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Get batch dividend history */
  getBatchDividends(options: GetBatchDividendsOptions): Promise<models.GqlDividendsBatch> {
    return this.transport.request<models.GqlDividendsBatch>("GET", "/v2/dividends", {
      query: queryOf(options),
    });
  }

  /**
   * Get batch dividend history
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterBatchDividends(options: GetBatchDividendsIterOptions): AsyncPager<unknown> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<unknown>((cursor) =>
      this.transport.request("GET", "/v2/dividends", {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Get batch financial statements */
  getBatchFinancials(options: GetBatchFinancialsOptions): Promise<models.GqlFinancialsBatch> {
    return this.transport.request<models.GqlFinancialsBatch>("GET", "/v2/financials", {
      query: queryOf(options),
    });
  }

  /** Get batch technical indicators */
  getBatchIndicators(options: GetBatchIndicatorsOptions): Promise<models.GqlIndicatorsBatch> {
    return this.transport.request<models.GqlIndicatorsBatch>("GET", "/v2/indicators", {
      query: queryOf(options),
    });
  }

  /**
   * Get batch technical indicators
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterBatchIndicators(options: GetBatchIndicatorsIterOptions): AsyncPager<unknown> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<unknown>((cursor) =>
      this.transport.request("GET", "/v2/indicators", {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Get batch options chains */
  getBatchOptions(options: GetBatchOptionsOptions): Promise<models.GqlOptionsBatch> {
    return this.transport.request<models.GqlOptionsBatch>("GET", "/v2/options", {
      query: queryOf(options),
    });
  }

  /**
   * Get batch options chains
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterBatchOptions(options: GetBatchOptionsIterOptions): AsyncPager<unknown> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<unknown>((cursor) =>
      this.transport.request("GET", "/v2/options", {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Get batch stock recommendations */
  getBatchRecommendations(options: GetBatchRecommendationsOptions): Promise<models.GqlRecommendationsBatch> {
    return this.transport.request<models.GqlRecommendationsBatch>("GET", "/v2/recommendations", {
      query: queryOf(options),
    });
  }

  /** Get batch stock split history */
  getBatchSplits(options: GetBatchSplitsOptions): Promise<models.GqlSplitsBatch> {
    return this.transport.request<models.GqlSplitsBatch>("GET", "/v2/splits", {
      query: queryOf(options),
    });
  }

  /** Get upcoming financial event calendar */
  getCalendar(options: GetCalendarOptions): Promise<models.GqlCalendarEvent[]> {
    return this.transport.request<models.GqlCalendarEvent[]>("GET", "/v2/calendar", {
      query: queryOf(options),
    });
  }

  /** Get capital gains distribution history */
  getCapitalGains(symbol: string, options: GetCapitalGainsOptions = {}): Promise<models.GqlCapitalGain[]> {
    return this.transport.request<models.GqlCapitalGain[]>("GET", `/v2/capital-gains/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Get historical chart data */
  getChart(symbol: string, options: GetChartOptions = {}): Promise<models.GqlChart> {
    return this.transport.request<models.GqlChart>("GET", `/v2/chart/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /**
   * Get historical chart data
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterChart(symbol: string, options: GetChartIterOptions = {}): AsyncPager<unknown> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<unknown>((cursor) =>
      this.transport.request("GET", `/v2/chart/${pathSeg(symbol)}`, {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Weekly CFTC Commitments of Traders positioning (keyless) */
  getCommitmentsOfTraders(symbol: string, options: GetCommitmentsOfTradersOptions = {}): Promise<models.GqlCommitmentsOfTraders> {
    return this.transport.request<models.GqlCommitmentsOfTraders>("GET", `/v2/cftc/cot/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Get company profile */
  getCompanyProfile(symbol: string, options: GetCompanyProfileOptions = {}): Promise<models.GqlCompanyProfile> {
    return this.transport.request<models.GqlCompanyProfile>("GET", `/v2/company-profile/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Congressional (senate) trading disclosures for a symbol */
  getCongressionalTrades(symbol: string, options: GetCongressionalTradesOptions = {}): Promise<models.GqlCongressionalTrade[]> {
    return this.transport.request<models.GqlCongressionalTrade[]>("GET", `/v2/filings/${pathSeg(symbol)}/congressional-trades`, {
      query: queryOf(options),
    });
  }

  /** Single coin by CoinGecko ID */
  getCryptoCoin(id: string, options: GetCryptoCoinOptions = {}): Promise<models.GqlCoinQuote> {
    return this.transport.request<models.GqlCoinQuote>("GET", `/v2/crypto/coins/${pathSeg(id)}`, {
      query: queryOf(options),
    });
  }

  /** Top coins by market cap */
  getCryptoCoins(options: GetCryptoCoinsOptions = {}): Promise<models.GqlCoinQuote[]> {
    return this.transport.request<models.GqlCoinQuote[]>("GET", "/v2/crypto/coins", {
      query: queryOf(options),
    });
  }

  /**
   * Top coins by market cap
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterCryptoCoins(options: GetCryptoCoinsIterOptions = {}): AsyncPager<models.GqlCoinQuote> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<models.GqlCoinQuote>((cursor) =>
      this.transport.request("GET", "/v2/crypto/coins", {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Aggregate global cryptocurrency market statistics */
  getCryptoGlobal(options: GetCryptoGlobalOptions = {}): Promise<models.GqlGlobalCryptoStats> {
    return this.transport.request<models.GqlGlobalCryptoStats>("GET", "/v2/crypto/global", {
      query: queryOf(options),
    });
  }

  /** Market-wide crypto news */
  getCryptoNews(options: GetCryptoNewsOptions = {}): Promise<models.GqlNews[]> {
    return this.transport.request<models.GqlNews[]>("GET", "/v2/crypto/news", {
      query: queryOf(options),
    });
  }

  /** Search CoinGecko's coin universe by free-text query */
  getCryptoSearch(options: GetCryptoSearchOptions): Promise<models.GqlSymbolMatch[]> {
    return this.transport.request<models.GqlSymbolMatch[]>("GET", "/v2/crypto/search", {
      query: queryOf(options),
    });
  }

  /** Coins trending on CoinGecko over the last 24h */
  getCryptoTrending(options: GetCryptoTrendingOptions = {}): Promise<models.GqlTrendingCoin[]> {
    return this.transport.request<models.GqlTrendingCoin[]>("GET", "/v2/crypto/trending", {
      query: queryOf(options),
    });
  }

  /** Get available currencies */
  getCurrencies(options: GetCurrenciesOptions = {}): Promise<models.GqlCurrency[]> {
    return this.transport.request<models.GqlCurrency[]>("GET", "/v2/currencies", {
      query: queryOf(options),
    });
  }

  /** Get dividend history and analytics */
  getDividends(symbol: string, options: GetDividendsOptions = {}): Promise<models.GqlDividends> {
    return this.transport.request<models.GqlDividends>("GET", `/v2/dividends/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /**
   * Get dividend history and analytics
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterDividends(symbol: string, options: GetDividendsIterOptions = {}): AsyncPager<unknown> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<unknown>((cursor) =>
      this.transport.request("GET", `/v2/dividends/${pathSeg(symbol)}`, {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Get earnings-surprise history */
  getEarningsSurprises(symbol: string, options: GetEarningsSurprisesOptions = {}): Promise<models.GqlEarningsSurpriseHistory> {
    return this.transport.request<models.GqlEarningsSurpriseHistory>("GET", `/v2/earnings-surprises/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Get earnings call transcript */
  getEarningsTranscript(symbol: string, options: GetEarningsTranscriptOptions = {}): Promise<models.GqlEarningsTranscript> {
    return this.transport.request<models.GqlEarningsTranscript>("GET", `/v2/earnings-transcript/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Resolve ticker to CIK */
  getEdgarCik(symbol: string, options: GetEdgarCikOptions = {}): Promise<models.GqlEdgarCik> {
    return this.transport.request<models.GqlEdgarCik>("GET", `/v2/edgar/cik/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Get XBRL financial data */
  getEdgarFacts(symbol: string, options: GetEdgarFactsOptions = {}): Promise<models.GqlFactConcept[]> {
    return this.transport.request<models.GqlFactConcept[]>("GET", `/v2/edgar/facts/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /**
   * Get XBRL financial data
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterEdgarFacts(symbol: string, options: GetEdgarFactsIterOptions = {}): AsyncPager<models.GqlFactConcept> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<models.GqlFactConcept>((cursor) =>
      this.transport.request("GET", `/v2/edgar/facts/${pathSeg(symbol)}`, {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Search SEC filings */
  getEdgarSearch(options: GetEdgarSearchOptions): Promise<models.GqlEdgarSearchResults> {
    return this.transport.request<models.GqlEdgarSearchResults>("GET", "/v2/edgar/search", {
      query: queryOf(options),
    });
  }

  /** Get SEC filing history */
  getEdgarSubmissions(symbol: string, options: GetEdgarSubmissionsOptions = {}): Promise<models.GqlEdgarSubmissions> {
    return this.transport.request<models.GqlEdgarSubmissions>("GET", `/v2/edgar/submissions/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /**
   * Get SEC filing history
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterEdgarSubmissions(symbol: string, options: GetEdgarSubmissionsIterOptions = {}): AsyncPager<unknown> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<unknown>((cursor) =>
      this.transport.request("GET", `/v2/edgar/submissions/${pathSeg(symbol)}`, {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Get supported exchanges */
  getExchanges(options: GetExchangesOptions = {}): Promise<models.GqlExchange[]> {
    return this.transport.request<models.GqlExchange[]>("GET", "/v2/exchanges", {
      query: queryOf(options),
    });
  }

  /** Fails-to-deliver records for a symbol */
  getFailsToDeliver(symbol: string, options: GetFailsToDeliverOptions = {}): Promise<models.GqlFailToDeliver[]> {
    return this.transport.request<models.GqlFailToDeliver[]>("GET", `/v2/filings/${pathSeg(symbol)}/fails-to-deliver`, {
      query: queryOf(options),
    });
  }

  /** Fear & Greed Index */
  getFearAndGreed(options: GetFearAndGreedOptions = {}): Promise<models.GqlFearAndGreed> {
    return this.transport.request<models.GqlFearAndGreed>("GET", "/v2/fear-and-greed", {
      query: queryOf(options),
    });
  }

  /** RSS/Atom news feeds */
  getFeeds(options: GetFeedsOptions = {}): Promise<models.GqlFeedEntry[]> {
    return this.transport.request<models.GqlFeedEntry[]>("GET", "/v2/feeds", {
      query: queryOf(options),
    });
  }

  /**
   * RSS/Atom news feeds
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterFeeds(options: GetFeedsIterOptions = {}): AsyncPager<models.GqlFeedEntry> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<models.GqlFeedEntry>((cursor) =>
      this.transport.request("GET", "/v2/feeds", {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Get financial statements */
  getFinancials(symbol: string, statement: models.StatementType, options: GetFinancialsOptions = {}): Promise<models.GqlFinancialLineItem[]> {
    return this.transport.request<models.GqlFinancialLineItem[]>("GET", `/v2/financials/${pathSeg(symbol)}/${pathSeg(statement)}`, {
      query: queryOf(options),
    });
  }

  /** Market-wide forex news */
  getForexNews(options: GetForexNewsOptions = {}): Promise<models.GqlNews[]> {
    return this.transport.request<models.GqlNews[]>("GET", "/v2/forex/news", {
      query: queryOf(options),
    });
  }

  /** FRED time series */
  getFredSeries(id: string, options: GetFredSeriesOptions = {}): Promise<models.GqlMacroSeries> {
    return this.transport.request<models.GqlMacroSeries>("GET", `/v2/fred/series/${pathSeg(id)}`, {
      query: queryOf(options),
    });
  }

  /**
   * FRED time series
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterFredSeries(id: string, options: GetFredSeriesIterOptions = {}): AsyncPager<unknown> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<unknown>((cursor) =>
      this.transport.request("GET", `/v2/fred/series/${pathSeg(id)}`, {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** US Treasury yield curve */
  getFredTreasuryYields(options: GetFredTreasuryYieldsOptions = {}): Promise<models.GqlTreasuryYield[]> {
    return this.transport.request<models.GqlTreasuryYield[]>("GET", "/v2/fred/treasury-yields", {
      query: queryOf(options),
    });
  }

  /**
   * US Treasury yield curve
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterFredTreasuryYields(options: GetFredTreasuryYieldsIterOptions = {}): AsyncPager<models.GqlTreasuryYield> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<models.GqlTreasuryYield>((cursor) =>
      this.transport.request("GET", "/v2/fred/treasury-yields", {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Worldwide news mentioning a symbol (GDELT, keyless) */
  getGdeltNews(symbol: string, options: GetGdeltNewsOptions = {}): Promise<models.GqlNews[]> {
    return this.transport.request<models.GqlNews[]>("GET", `/v2/gdelt/news/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Get general market news */
  getGeneralNews(options: GetGeneralNewsOptions = {}): Promise<models.GqlNews[]> {
    return this.transport.request<models.GqlNews[]>("GET", "/v2/news", {
      query: queryOf(options),
    });
  }

  /**
   * Get general market news
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterGeneralNews(options: GetGeneralNewsIterOptions = {}): AsyncPager<models.GqlNews> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<models.GqlNews>((cursor) =>
      this.transport.request("GET", "/v2/news", {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Get holder data */
  getHolders(symbol: string, type: models.HolderType, options: GetHoldersOptions = {}): Promise<models.HoldersResponse> {
    return this.transport.request<models.HoldersResponse>("GET", `/v2/holders/${pathSeg(symbol)}/${pathSeg(type)}`, {
      query: queryOf(options),
    });
  }

  /**
   * Get holder data
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterHolders(symbol: string, type: models.HolderType, options: GetHoldersIterOptions = {}): AsyncPager<unknown> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<unknown>((cursor) =>
      this.transport.request("GET", `/v2/holders/${pathSeg(symbol)}/${pathSeg(type)}`, {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Get market hours */
  getHours(options: GetHoursOptions = {}): Promise<models.GqlMarketHours> {
    return this.transport.request<models.GqlMarketHours>("GET", "/v2/hours", {
      query: queryOf(options),
    });
  }

  /** Get technical indicators */
  getIndicators(symbol: string, options: GetIndicatorsOptions = {}): Promise<models.GqlIndicatorsSummary> {
    return this.transport.request<models.GqlIndicatorsSummary>("GET", `/v2/indicators/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Get world market indices */
  getIndices(options: GetIndicesOptions = {}): Promise<models.GqlQuote[]> {
    return this.transport.request<models.GqlQuote[]>("GET", "/v2/indices", {
      query: queryOf(options),
    });
  }

  /** Get industry data */
  getIndustry(industry: models.Industry, options: GetIndustryOptions = {}): Promise<models.GqlIndustryData> {
    return this.transport.request<models.GqlIndustryData>("GET", `/v2/industries/${pathSeg(industry)}`, {
      query: queryOf(options),
    });
  }

  /** Get market summary */
  getMarketSummary(options: GetMarketSummaryOptions = {}): Promise<models.GqlMarketSummaryQuote[]> {
    return this.transport.request<models.GqlMarketSummaryQuote[]>("GET", "/v2/market-summary", {
      query: queryOf(options),
    });
  }

  /** Prometheus metrics */
  getMetrics(): Promise<models.MetricsText> {
    return this.transport.request<models.MetricsText>("GET", "/v2/metrics");
  }

  /** Get news for a symbol */
  getNews(symbol: string, options: GetNewsOptions = {}): Promise<models.GqlNews[]> {
    return this.transport.request<models.GqlNews[]>("GET", `/v2/news/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /**
   * Get news for a symbol
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterNews(symbol: string, options: GetNewsIterOptions = {}): AsyncPager<models.GqlNews> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<models.GqlNews>((cursor) =>
      this.transport.request("GET", `/v2/news/${pathSeg(symbol)}`, {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Get options chain */
  getOptions(symbol: string, options: GetOptionsOptions = {}): Promise<models.GqlOptions> {
    return this.transport.request<models.GqlOptions>("GET", `/v2/options/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /**
   * Get options chain
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterOptions(symbol: string, options: GetOptionsIterOptions = {}): AsyncPager<unknown> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<unknown>((cursor) =>
      this.transport.request("GET", `/v2/options/${pathSeg(symbol)}`, {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Get quote for a symbol */
  getQuote(symbol: string, options: GetQuoteOptions = {}): Promise<models.GqlQuote> {
    return this.transport.request<models.GqlQuote>("GET", `/v2/quote/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Get quote type */
  getQuoteType(symbol: string, options: GetQuoteTypeOptions = {}): Promise<models.GqlQuoteTypeData> {
    return this.transport.request<models.GqlQuoteTypeData>("GET", `/v2/quote-type/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Get quotes for multiple symbols */
  getQuotes(options: GetQuotesOptions): Promise<models.GqlQuotesBatch> {
    return this.transport.request<models.GqlQuotesBatch>("GET", "/v2/quotes", {
      query: queryOf(options),
    });
  }

  /**
   * Get quotes for multiple symbols
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterQuotes(options: GetQuotesIterOptions): AsyncPager<unknown> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<unknown>((cursor) =>
      this.transport.request("GET", "/v2/quotes", {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }

  /** Get similar stock recommendations */
  getRecommendations(symbol: string, options: GetRecommendationsOptions = {}): Promise<models.GqlRecommendation> {
    return this.transport.request<models.GqlRecommendation>("GET", `/v2/recommendations/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Risk analytics */
  getRisk(symbol: string, options: GetRiskOptions = {}): Promise<models.GqlRiskSummary> {
    return this.transport.request<models.GqlRiskSummary>("GET", `/v2/risk/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Get screener results */
  getScreeners(screener: models.Screener, options: GetScreenersOptions = {}): Promise<models.GqlScreenerResults> {
    return this.transport.request<models.GqlScreenerResults>("GET", `/v2/screeners/${pathSeg(screener)}`, {
      query: queryOf(options),
    });
  }

  /** Get sector data */
  getSector(sector: models.Sector, options: GetSectorOptions = {}): Promise<models.GqlSectorData> {
    return this.transport.request<models.GqlSectorData>("GET", `/v2/sectors/${pathSeg(sector)}`, {
      query: queryOf(options),
    });
  }

  /** Get batch sparkline data */
  getSpark(options: GetSparkOptions): Promise<models.GqlSparkBatch> {
    return this.transport.request<models.GqlSparkBatch>("GET", "/v2/spark", {
      query: queryOf(options),
    });
  }

  /** Get stock split history */
  getSplits(symbol: string, options: GetSplitsOptions = {}): Promise<models.GqlSplit[]> {
    return this.transport.request<models.GqlSplit[]>("GET", `/v2/splits/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Get earnings transcript */
  getTranscript(symbol: string, options: GetTranscriptOptions = {}): Promise<models.GqlTranscript> {
    return this.transport.request<models.GqlTranscript>("GET", `/v2/transcripts/${pathSeg(symbol)}`, {
      query: queryOf(options),
    });
  }

  /** Get all earnings transcripts */
  getTranscripts(symbol: string, options: GetTranscriptsOptions = {}): Promise<models.GqlTranscriptWithMeta[]> {
    return this.transport.request<models.GqlTranscriptWithMeta[]>("GET", `/v2/transcripts/${pathSeg(symbol)}/all`, {
      query: queryOf(options),
    });
  }

  /** Get trending tickers */
  getTrending(options: GetTrendingOptions = {}): Promise<models.GqlTrendingQuote[]> {
    return this.transport.request<models.GqlTrendingQuote[]>("GET", "/v2/trending", {
      query: queryOf(options),
    });
  }

  /** Health check */
  healthCheck(): Promise<models.HealthResponse> {
    return this.transport.request<models.HealthResponse>("GET", "/v2/health");
  }

  /** Look up symbols by type */
  lookup(options: LookupOptions): Promise<models.GqlLookupResults> {
    return this.transport.request<models.GqlLookupResults>("GET", "/v2/lookup", {
      query: queryOf(options),
    });
  }

  /** Ping endpoint */
  ping(): Promise<models.PingResponse> {
    return this.transport.request<models.PingResponse>("GET", "/v2/ping");
  }

  /** Custom screener query */
  postCustomScreener(body: models.CustomScreenerRequest): Promise<models.GqlScreenerResults> {
    return this.transport.request<models.GqlScreenerResults>("POST", "/v2/screeners/custom", {
      body,
    });
  }

  /** Search symbols and companies */
  search(options: SearchOptions): Promise<models.GqlSearchResults> {
    return this.transport.request<models.GqlSearchResults>("GET", "/v2/search", {
      query: queryOf(options),
    });
  }

  /**
   * Search symbols and companies
   *
   * Auto-paginates; iterate items, or call `.pages()` for whole pages.
   */
  iterSearch(options: SearchIterOptions): AsyncPager<unknown> {
    const { limit: pageLimit = 50, ...rest } = options;
    return new AsyncPager<unknown>((cursor) =>
      this.transport.request("GET", "/v2/search", {
        query: queryOf({ ...rest, limit: pageLimit, cursor: cursor }),
      }),
    );
  }
}
