// Code generated by `cargo soothfast sdk gen`. DO NOT EDIT.
/** Wire models for finance-query. */

/** Request body for `POST /v2/screeners/custom`. */
export interface CustomScreenerRequest {
  /** Comma-separated list of fields to include in response */
  fields?: string;
  /** Filter conditions */
  filters?: FilterCondition[];
  /** Value format: raw, pretty, or both (default: raw) */
  format?: ValueFormat;
  /** Pagination offset (default: 0) */
  offset?: number;
  /** Quote type: EQUITY, ETF, MUTUALFUND, etc. (default: EQUITY) */
  quoteType?: QuoteType;
  /** Number of results (default: 25, max: 250) */
  size?: number;
  /** Field to sort by (default: intradaymarketcap) */
  sortField?: string;
  /** Sort direction: "ASC" or "DESC" (default: DESC) */
  sortType?: SortType;
}

/** A single custom-screener filter condition. */
export interface FilterCondition {
  /** Field name (e.g., "region", "avgdailyvol3m", "intradaymarketcap") */
  field: string;
  /** Operator: eq, gt, gte, lt, lte, btwn */
  operator: string;
  /** Value(s) for the condition */
  value: unknown;
}

export interface GqlAroonData {
  aroonDown?: number;
  aroonUp?: number;
}

/** A single symbol's fetch failure within a batch root field. */
export interface GqlBatchError {
  message: string;
  symbol: string;
}

export interface GqlBenchmarkPerformance {
  dayChangePercent?: number;
  fiveYearChangePercent?: number;
  name?: string;
  oneYearChangePercent?: number;
  threeYearChangePercent?: number;
  ytdChangePercent?: number;
}

export interface GqlBollingerBandsData {
  lower?: number;
  middle?: number;
  upper?: number;
}

export interface GqlBullBearPowerData {
  bearPower?: number;
  bullPower?: number;
}

/** A single upcoming financial event. */
export interface GqlCalendarEvent {
  date: string;
  event: GqlEventKind;
  symbol?: string;
  timestamp: number;
}

/** A single OHLCV candle / price bar. */
export interface GqlCandle {
  adjClose?: number;
  close: number;
  high: number;
  low: number;
  open: number;
  timestamp: number;
  volume: number;
}

/** A single capital gain distribution. */
export interface GqlCapitalGain {
  amount: number;
  timestamp: number;
}

/** Result of the batch `capitalGainsBatch` root field: successfully fetched capital gains histories plus any per-symbol fetch errors. */
export interface GqlCapitalGainsBatch {
  capitalGains: GqlSymbolCapitalGains[];
  errors: GqlBatchError[];
}

/** Historical chart data for a single symbol. */
export interface GqlChart {
  /** OHLCV candles for the requested interval/range. */
  candles: Page_GqlCandle;
  /** Time interval used for this chart (e.g. "1d"). */
  interval?: string;
  meta: GqlChartMeta;
  /** Time range used for this chart (e.g. "1mo"). */
  range?: string;
  symbol: string;
}

/** Metadata associated with a chart response. */
export interface GqlChartMeta {
  chartPreviousClose?: number;
  currency?: string;
  dataGranularity?: string;
  exchangeName?: string;
  exchangeTimezoneName?: string;
  fiftyTwoWeekHigh?: number;
  fiftyTwoWeekLow?: number;
  firstTradeDate?: number;
  fullExchangeName?: string;
  gmtOffset?: number;
  hasPrePostMarketData?: boolean;
  instrumentType?: string;
  previousClose?: number;
  priceHint?: number;
  range?: string;
  regularMarketDayHigh?: number;
  regularMarketDayLow?: number;
  regularMarketPrice?: number;
  regularMarketTime?: number;
  regularMarketVolume?: number;
  symbol: string;
  timezone?: string;
}

/** Result of the batch `charts` root field: successfully fetched charts plus any per-symbol fetch errors. */
export interface GqlChartsBatch {
  /** Successfully fetched per-symbol charts. */
  charts: Page_GqlSymbolChart;
  errors: GqlBatchError[];
}

/** A cryptocurrency quote from CoinGecko, mirroring `finance_query::CoinQuote`, which has no serde rename of its own (plain snake_case JSON keys) — must not rename for deserialization either, even though GraphQL field names are camelCase. */
export interface GqlCoinQuote {
  circulatingSupply?: number;
  currentPrice?: number;
  id: string;
  image?: string;
  marketCap?: number;
  marketCapRank?: number;
  name: string;
  priceChangePercentage24H?: number;
  symbol: string;
  totalVolume?: number;
}

/** Mirrors `finance_query::cftc::CommitmentsOfTraders`. */
export interface GqlCommitmentsOfTraders {
  /** CFTC contract market code identifying this market. */
  cftcContractMarketCode: string;
  /** CFTC's own market and exchange name. */
  marketAndExchangeName: string;
  /** Weekly report rows, oldest first. */
  observations: Page_GqlCotObservation;
  /** The symbol the series was requested with (`"GC=F"`, or a raw CFTC code). */
  symbol: string;
}

/** Mirrors `finance_query::CommodityQuote`. */
export interface GqlCommodityQuote {
  change?: number;
  changePercent?: number;
  name?: string;
  price?: number;
  symbol: string;
  timestamp?: number;
  unit?: string;
}

/** Mirrors `finance_query::CompanyProfile`, which has no serde rename of its own — this deserializes snake_case keys while its GraphQL name stays camelCase. */
export interface GqlCompanyProfile {
  assetType?: string;
  country?: string;
  currency?: string;
  description?: string;
  exchange?: string;
  industry?: string;
  marketCapitalization?: number;
  name?: string;
  sector?: string;
  symbol?: string;
}

/** Mirrors `finance_query::CongressionalTrade`, which has no serde rename of its own — this deserializes snake_case keys while its GraphQL name stays camelCase. */
export interface GqlCongressionalTrade {
  amount?: string;
  assetDescription?: string;
  disclosureDate?: string;
  district?: string;
  firstName?: string;
  lastName?: string;
  link?: string;
  office?: string;
  symbol?: string;
  tradeType?: string;
  transactionDate?: string;
}

/** Mirrors `finance_query::cftc::CotObservation` — one weekly report row, broken down by trader category. */
export interface GqlCotObservation {
  managedMoneyLong?: number;
  managedMoneyShort?: number;
  managedMoneySpread?: number;
  nonreportableLong?: number;
  nonreportableShort?: number;
  openInterest?: number;
  otherReportableLong?: number;
  otherReportableShort?: number;
  otherReportableSpread?: number;
  producerMerchantLong?: number;
  producerMerchantShort?: number;
  /** Report date (`YYYY-MM-DD`) — the Tuesday the report is as of. */
  reportDate: string;
  swapDealerLong?: number;
  swapDealerShort?: number;
  swapDealerSpread?: number;
  totalReportableLong?: number;
  totalReportableShort?: number;
}

/** A currency pair supported by Yahoo Finance. */
export interface GqlCurrency {
  localLongName?: string;
  longName?: string;
  shortName?: string;
  symbol?: string;
}

/** A single dividend payment. */
export interface GqlDividend {
  amount: number;
  timestamp: number;
}

/** Computed dividend analytics. */
export interface GqlDividendAnalytics {
  averagePayment: number;
  cagr?: number;
  firstPayment?: GqlDividend;
  lastPayment?: GqlDividend;
  paymentCount: number;
  totalPaid: number;
}

/** Dividends response: payment history + computed analytics. */
export interface GqlDividends {
  analytics: GqlDividendAnalytics;
  /** Dividend payment history. */
  dividends: Page_GqlDividend;
}

/** Result of the batch `dividendsBatch` root field: successfully fetched dividend histories plus any per-symbol fetch errors. */
export interface GqlDividendsBatch {
  /** Successfully fetched per-symbol dividend histories. */
  dividends: Page_GqlSymbolDividends;
  errors: GqlBatchError[];
}

export interface GqlDonchianChannelsData {
  lower?: number;
  middle?: number;
  upper?: number;
}

export interface GqlEarningsSurprise {
  actualEps?: number;
  date?: string;
  estimatedEps?: number;
  surprise?: number;
  surprisePercent?: number;
  symbol?: string;
}

export interface GqlEarningsSurpriseHistory {
  surprises: GqlEarningsSurprise[];
}

export interface GqlEarningsTranscript {
  date?: string;
  quarter?: string;
  symbol?: string;
  text: string;
  year?: number;
}

/** Result of resolving a ticker symbol to its SEC CIK number. */
export interface GqlEdgarCik {
  cik: number;
  symbol: string;
}

export interface GqlEdgarFiling {
  accessionNumber: string;
  filingDate: string;
  form: string;
  primaryDocDescription: string;
  primaryDocument: string;
  reportDate: string;
  size: number;
}

export interface GqlEdgarSearchHit {
  adsh?: string;
  ciks: string[];
  displayNames: string[];
  fileDate?: string;
  form?: string;
}

export interface GqlEdgarSearchResults {
  hits: GqlEdgarSearchHit[];
  pageInfo?: GqlPageInfo;
  totalHits?: number;
}

export interface GqlEdgarSubmissions {
  category?: string;
  cik?: string;
  ein?: string;
  entityType?: string;
  exchanges: string[];
  /** Filing history. Can be large (hundreds+ of filings for established companies) — paginated. */
  filings: Page_GqlEdgarFiling;
  fiscalYearEnd?: string;
  insiderTransactionForIssuerExists?: number;
  insiderTransactionForOwnerExists?: number;
  name?: string;
  sic?: string;
  sicDescription?: string;
  stateOfIncorporation?: string;
  tickers: string[];
  website?: string;
}

export interface GqlElderRayData {
  bearPower?: number;
  bullPower?: number;
}

/** Mirrors `finance_query::EtfCountryWeighting`. */
export interface GqlEtfCountryWeighting {
  country?: string;
  weight?: number;
}

/** Mirrors `finance_query::EtfHolding`. */
export interface GqlEtfHolding {
  description?: string;
  symbol?: string;
  weight?: number;
}

/** Mirrors `finance_query::EtfProfile`. */
export interface GqlEtfProfile {
  assetType?: string;
  countryWeightings: GqlEtfCountryWeighting[];
  dividendYield?: number;
  holdings: GqlEtfHolding[];
  inceptionDate?: string;
  name?: string;
  netAssets?: number;
  netExpenseRatio?: number;
  portfolioTurnover?: number;
  sectorWeightings: GqlEtfSectorWeighting[];
  symbol?: string;
}

/** Mirrors `finance_query::EtfSectorWeighting`. */
export interface GqlEtfSectorWeighting {
  sector?: string;
  weight?: number;
}

/** A supported stock exchange with its symbol suffix and data provider. */
export interface GqlExchange {
  country: string;
  dataProvider: string;
  delay: string;
  market: string;
  suffix: string;
}

/** A single XBRL fact concept with its data points. */
export interface GqlFactConcept {
  concept: string;
  /** Reported data points for this concept. */
  dataPoints: Page_GqlFactDataPoint;
  description?: string;
  label?: string;
  taxonomy: string;
  unit: string;
}

export interface GqlFactDataPoint {
  accn?: string;
  end?: string;
  filed?: string;
  form?: string;
  fp?: string;
  frame?: string;
  fy?: number;
  start?: string;
  val?: number;
}

/** Mirrors `finance_query::FailToDeliver`, which has no serde rename of its own — this deserializes snake_case keys while its GraphQL name stays camelCase. */
export interface GqlFailToDeliver {
  date?: string;
  description?: string;
  name?: string;
  price?: number;
  quantity?: number;
  symbol?: string;
}

/** Fear & Greed index response, mirroring `finance_query::models::sentiment::FearAndGreed`. */
export interface GqlFearAndGreed {
  classification?: string;
  timestamp?: number;
  value?: number;
}

/** A single entry from an RSS/Atom feed. */
export interface GqlFeedEntry {
  published?: string;
  source: string;
  summary?: string;
  title: string;
  url: string;
}

/** Mirrors `finance_query::indicators::FibonacciLevels`, which has no serde rename of its own (plain snake_case keys). */
export interface GqlFibonacciLevels {
  level236?: number;
  level382?: number;
  level50?: number;
  level618?: number;
  level786?: number;
  swingHigh?: number;
  swingLow?: number;
}

/** Mirrors `finance_query::FilingSection`. */
export interface GqlFilingSection {
  content?: string;
  section?: string;
}

/** A single data point in a financial time series. */
export interface GqlFinancialDataPoint {
  /** Date string (e.g., "2024-09-30"). */
  date: string;
  /** Numeric value. */
  value: number;
}

/** A single line item in a financial statement (e.g., "TotalRevenue"). */
export interface GqlFinancialLineItem {
  /** Metric name (e.g., "TotalRevenue", "NetIncome"). */
  metric: string;
  /** Time-series values: date → value pairs. */
  values: GqlFinancialDataPoint[];
}

/** Result of the batch `financialsBatch` root field: successfully fetched statements plus any per-symbol fetch errors. */
export interface GqlFinancialsBatch {
  errors: GqlBatchError[];
  financials: GqlSymbolFinancials[];
}

/** Mirrors `finance_query::ForexQuote`. */
export interface GqlForexQuote {
  ask?: number;
  baseCurrency?: string;
  bid?: number;
  change?: number;
  changePercent?: number;
  price?: number;
  quoteCurrency?: string;
  symbol: string;
  timestamp?: number;
}

/** The `{raw, fmt, longFmt}` wrapper as it reaches the wire under `format=both`. */
export interface GqlFormattedObject {
  /** Human-readable rendering, e.g. `"182.50"` or `"3.50T"`. */
  fmt?: string;
  /** Fully expanded rendering for large numbers, e.g. `"3,500,000,000,000"`. */
  longFmt?: string;
  /** The underlying datum. May be `null` when upstream reports no value. */
  raw: unknown;
}

/** Mirrors `finance_query::FuturesQuote`. */
export interface GqlFuturesQuote {
  change?: number;
  changePercent?: number;
  exchange?: string;
  expirationDate?: string;
  name?: string;
  openInterest?: number;
  price?: number;
  symbol: string;
  timestamp?: number;
  underlying?: string;
  volume?: number;
}

/** Mirrors `finance_query::crypto::GlobalCryptoStats`. */
export interface GqlGlobalCryptoStats {
  activeCryptocurrencies?: number;
  btcDominance?: number;
  ethDominance?: number;
  marketCapChangePercentage24HUsd?: number;
  markets?: number;
  totalMarketCapUsd?: number;
  totalVolumeUsd?: number;
}

export interface GqlGrowthCompany {
  growthEstimate?: number;
  lastPrice?: number;
  name?: string;
  symbol: string;
  ytdReturn?: number;
}

/** Mirrors `finance_query::Candle` (camelCase serde keys). */
export interface GqlHeikinAshiCandle {
  adjClose?: number;
  close?: number;
  high?: number;
  low?: number;
  open?: number;
  timestamp?: number;
  volume?: number;
}

export interface GqlIchimokuData {
  baseLine?: number;
  conversionLine?: number;
  laggingSpan?: number;
  leadingSpanA?: number;
  leadingSpanB?: number;
}

/** Mirrors `finance_query::IndexConstituent`. */
export interface GqlIndexConstituent {
  cik?: string;
  dateFirstAdded?: string;
  founded?: string;
  headquarters?: string;
  name?: string;
  sector?: string;
  subSector?: string;
  symbol: string;
}

/** Result of the batch `indicatorsBatch` root field: successfully computed indicators plus any per-symbol fetch errors. */
export interface GqlIndicatorsBatch {
  errors: GqlBatchError[];
  /** Successfully computed per-symbol indicators. */
  indicators: Page_GqlSymbolIndicators;
}

/** All technical indicators for a symbol (latest values). */
export interface GqlIndicatorsSummary {
  accumulationDistribution?: number;
  adx14?: number;
  alma9?: number;
  aroon?: GqlAroonData;
  atr14?: number;
  awesomeOscillator?: number;
  balanceOfPower?: number;
  bollingerBands?: GqlBollingerBandsData;
  bullBearPower?: GqlBullBearPowerData;
  cci20?: number;
  chaikinOscillator?: number;
  choppinessIndex14?: number;
  cmf20?: number;
  cmo14?: number;
  coppockCurve?: number;
  dema20?: number;
  donchianChannels?: GqlDonchianChannelsData;
  elderRayIndex?: GqlElderRayData;
  ema10?: number;
  ema100?: number;
  ema20?: number;
  ema200?: number;
  ema50?: number;
  fibonacciPivotPoints?: GqlPivotPointsData;
  fibonacciRetracement50?: GqlFibonacciLevels;
  heikinAshi?: GqlHeikinAshiCandle;
  hma20?: number;
  ichimoku?: GqlIchimokuData;
  keltnerChannels?: GqlKeltnerChannelsData;
  macd?: GqlMacdData;
  mcginleyDynamic20?: number;
  mfi14?: number;
  momentum10?: number;
  obv?: number;
  parabolicSar?: number;
  pivotPoints?: GqlPivotPointsData;
  roc12?: number;
  rsi14?: number;
  sma10?: number;
  sma100?: number;
  sma20?: number;
  sma200?: number;
  sma50?: number;
  stochastic?: GqlStochasticData;
  stochasticRsi?: GqlStochasticData;
  supertrend?: GqlSuperTrendData;
  tema20?: number;
  trueRange?: number;
  vwap?: number;
  vwma20?: number;
  williamsR14?: number;
  wma10?: number;
  wma100?: number;
  wma20?: number;
  wma200?: number;
  wma50?: number;
  zigzagLastPivot?: GqlZigZagPoint;
}

export interface GqlIndustryCompany {
  dayChangePercent?: number;
  lastPrice?: number;
  marketCap?: number;
  marketWeight?: number;
  name?: string;
  rating?: string;
  symbol: string;
  targetPrice?: number;
  ytdReturn?: number;
}

export interface GqlIndustryData {
  benchmark?: GqlBenchmarkPerformance;
  key: string;
  name: string;
  overview?: GqlIndustryOverview;
  performance?: GqlIndustryPerformance;
  researchReports: GqlIndustryResearchReport[];
  sectorKey?: string;
  sectorName?: string;
  symbol?: string;
  topCompanies: GqlIndustryCompany[];
  topGrowthCompanies: GqlGrowthCompany[];
  topPerformingCompanies: GqlPerformingCompany[];
}

export interface GqlIndustryOverview {
  companiesCount?: number;
  description?: string;
  employeeCount?: number;
  marketCap?: number;
  marketWeight?: number;
}

export interface GqlIndustryPerformance {
  dayChangePercent?: number;
  fiveYearChangePercent?: number;
  oneYearChangePercent?: number;
  threeYearChangePercent?: number;
  ytdChangePercent?: number;
}

export interface GqlIndustryResearchReport {
  id?: string;
  investmentRating?: string;
  provider?: string;
  reportDate?: string;
  reportType?: string;
  targetPrice?: number;
  targetPriceStatus?: string;
  title?: string;
}

export interface GqlKeltnerChannelsData {
  lower?: number;
  middle?: number;
  upper?: number;
}

/** A quote/document result from `lookup`. */
export interface GqlLookupQuote {
  companyLogoUrl?: string;
  exchDisp?: string;
  exchange?: string;
  industry?: string;
  logoUrl?: string;
  longName?: string;
  quoteType?: string;
  regularMarketChange?: number;
  regularMarketChangePercent?: number;
  regularMarketPreviousClose?: number;
  regularMarketPrice?: number;
  sector?: string;
  shortName?: string;
  symbol: string;
  typeDisp?: string;
}

/** Type-filtered symbol lookup results. */
export interface GqlLookupResults {
  count?: number;
  quotes: GqlLookupQuote[];
  start?: number;
}

export interface GqlMacdData {
  histogram?: number;
  macd?: number;
  signal?: number;
}

/** A single observation in a FRED data series. */
export interface GqlMacroObservation {
  date: string;
  value?: number;
}

/** A FRED macro-economic time series with all its observations. */
export interface GqlMacroSeries {
  id: string;
  /** Time series observations. */
  observations: Page_GqlMacroObservation;
}

/** A single market-wide calendar entry. */
export interface GqlMarketCalendarEntry {
  date?: string;
  detail: GqlMarketCalendarDetail;
  symbol?: string;
}

/** Market hours for one or more markets. */
export interface GqlMarketHours {
  markets: GqlMarketTime[];
}

/** A sector's aggregate price/earnings ratio, provider-routed via `Providers::market()`. */
export interface GqlMarketSectorPe {
  date?: string;
  exchange?: string;
  pe?: number;
  sector: string;
}

/** A sector's aggregate performance, provider-routed via `Providers::market()` (`Capability::MARKET`). Distinct from `GqlSectorPerformance` in `types/sector.rs`, which is a nested field on Yahoo-only `finance::sector`. */
export interface GqlMarketSectorPerformance {
  changePercent?: number;
  exchange?: string;
  sector: string;
}

/** A market summary quote (major index / currency / commodity). */
export interface GqlMarketSummaryQuote {
  exchange?: string;
  fullExchangeName?: string;
  marketState?: string;
  quoteType?: string;
  regularMarketChange?: GqlFormattedValue;
  regularMarketChangePercent?: GqlFormattedValue;
  regularMarketPreviousClose?: GqlFormattedValue;
  regularMarketPrice?: GqlFormattedValue;
  regularMarketTime?: GqlFormattedValue;
  shortName?: string;
  spark?: unknown;
  symbol?: string;
}

/** Market open/close status for a single market. */
export interface GqlMarketTime {
  close?: string;
  dst?: boolean;
  gmtOffset?: number;
  id: string;
  message?: string;
  name: string;
  open?: string;
  status: string;
  time?: string;
  timezone?: string;
  timezoneShort?: string;
}

/** A scraped news article. */
export interface GqlNews {
  img: string;
  link: string;
  /** Sentiment score for this article's title (VADER lexicon-based); `null` if the `sentiment` feature isn't compiled in. */
  sentiment?: GqlSentiment;
  source: string;
  time: string;
  title: string;
}

/** An option contract (call or put). */
export interface GqlOptionContract {
  ask?: number;
  bid?: number;
  change?: number;
  contractSize?: string;
  contractSymbol: string;
  currency?: string;
  expiration?: number;
  impliedVolatility?: number;
  inTheMoney?: boolean;
  lastPrice?: number;
  lastTradeDate?: number;
  openInterest?: number;
  percentChange?: number;
  strike: number;
  volume?: number;
}

/** Options chain data for a symbol. */
export interface GqlOptions {
  /** All call contracts across expirations. */
  calls: Page_GqlOptionContract;
  /** Available expiration dates (Unix timestamps). */
  expirationDates: number[];
  /** All put contracts across expirations. */
  puts: Page_GqlOptionContract;
  /** Available strike prices. */
  strikes: number[];
}

/** Result of the batch `optionsBatch` root field: successfully fetched options chains plus any per-symbol fetch errors. */
export interface GqlOptionsBatch {
  errors: GqlBatchError[];
  /** Successfully fetched per-symbol options chains. */
  options: Page_GqlSymbolOptions;
}

/** Pagination metadata for fields whose pagination genuinely happens at the *input* level (an `offset`/`size`-style arg the upstream API already consumes to fetch the requested slice directly — screener's `customScreener`, EDGAR's `edgarSearch`) rather than by fetching everything and slicing in Rust. These fields keep returning a plain `Vec<T>` (no `edges`/`node` wrapping — that would just be redundant pagination vocabulary layered on top of the offset/size that already did the real work) and add this as a `pageInfo` sibling field alongside their existing `total`/`totalHits`. */
export interface GqlPageInfo {
  endCursor?: string;
  hasNextPage: boolean;
  hasPreviousPage: boolean;
  startCursor?: string;
}

export interface GqlParagraph {
  end: number;
  sentences: GqlSentence[];
  speaker: number;
  start: number;
  text: string;
}

export interface GqlPerformingCompany {
  lastPrice?: number;
  name?: string;
  symbol: string;
  targetPrice?: number;
  ytdReturn?: number;
}

export interface GqlPivotPointsData {
  pivot?: number;
  r1?: number;
  r2?: number;
  r3?: number;
  s1?: number;
  s2?: number;
  s3?: number;
}

/** Mirrors `finance_query::PressRelease`. */
export interface GqlPressRelease {
  date?: string;
  symbol?: string;
  text?: string;
  title?: string;
}

/** Mirrors `finance_query::PriceTargetConsensus`. */
export interface GqlPriceTargetConsensus {
  symbol?: string;
  targetConsensus?: number;
  targetHigh?: number;
  targetLow?: number;
  targetMedian?: number;
}

/** Full quote data for a stock / ETF / fund, mirroring `finance_query::Quote`. */
export interface GqlQuote {
  allTimeHigh?: GqlFormattedValue;
  allTimeLow?: GqlFormattedValue;
  ask?: GqlFormattedValue;
  askSize?: GqlFormattedValue;
  auditRisk?: number;
  averageVolume?: GqlFormattedValue;
  balanceSheetHistory?: unknown;
  balanceSheetHistoryQuarterly?: unknown;
  beta?: GqlFormattedValue;
  bid?: GqlFormattedValue;
  bidSize?: GqlFormattedValue;
  boardRisk?: number;
  bookValue?: GqlFormattedValue;
  calendarEvents?: unknown;
  cashflowStatementHistory?: unknown;
  cashflowStatementHistoryQuarterly?: unknown;
  category?: string;
  city?: string;
  companyLogoUrl?: string;
  companyOfficers?: unknown;
  compensationRisk?: number;
  country?: string;
  currency?: string;
  currencySymbol?: string;
  currentPrice?: GqlFormattedValue;
  currentRatio?: GqlFormattedValue;
  dateShortInterest?: GqlFormattedValue;
  dayHigh?: GqlFormattedValue;
  dayLow?: GqlFormattedValue;
  debtToEquity?: GqlFormattedValue;
  dividendRate?: GqlFormattedValue;
  dividendYield?: GqlFormattedValue;
  earnings?: unknown;
  earningsGrowth?: GqlFormattedValue;
  earningsHistory?: unknown;
  earningsQuarterlyGrowth?: GqlFormattedValue;
  earningsTrend?: unknown;
  ebitdaMargins?: GqlFormattedValue;
  enterpriseToEbitda?: GqlFormattedValue;
  enterpriseToRevenue?: GqlFormattedValue;
  enterpriseValue?: GqlFormattedValue;
  equityPerformance?: unknown;
  exDividendDate?: GqlFormattedValue;
  exchange?: string;
  exchangeDataDelayedBy?: number;
  exchangeName?: string;
  fiftyDayAverage?: GqlFormattedValue;
  fiftyTwoWeekHigh?: GqlFormattedValue;
  fiftyTwoWeekLow?: GqlFormattedValue;
  financialCurrency?: string;
  firstTradeDateEpochUtc?: number;
  fiveYearAvgDividendYield?: GqlFormattedValue;
  floatShares?: GqlFormattedValue;
  forwardEps?: GqlFormattedValue;
  forwardPe?: GqlFormattedValue;
  freeCashflow?: GqlFormattedValue;
  fromCurrency?: string;
  fullTimeEmployees?: number;
  fundFamily?: string;
  fundOwnership?: unknown;
  fundPerformance?: unknown;
  fundProfile?: unknown;
  gmtOffSetMilliseconds?: number;
  grossMargins?: GqlFormattedValue;
  grossProfits?: GqlFormattedValue;
  heldPercentInsiders?: GqlFormattedValue;
  heldPercentInstitutions?: GqlFormattedValue;
  impliedSharesOutstanding?: GqlFormattedValue;
  incomeStatementHistory?: unknown;
  incomeStatementHistoryQuarterly?: unknown;
  indexTrend?: unknown;
  industry?: string;
  industryDisp?: string;
  industryKey?: string;
  industryTrend?: unknown;
  insiderHolders?: unknown;
  insiderTransactions?: unknown;
  institutionOwnership?: unknown;
  irWebsite?: string;
  lastDividendDate?: GqlFormattedValue;
  lastDividendValue?: GqlFormattedValue;
  lastFiscalYearEnd?: GqlFormattedValue;
  lastSplitDate?: GqlFormattedValue;
  lastSplitFactor?: string;
  logoUrl?: string;
  longBusinessSummary?: string;
  longName?: string;
  majorHoldersBreakdown?: unknown;
  marketCap?: GqlFormattedValue;
  marketState?: string;
  mostRecentQuarter?: GqlFormattedValue;
  navPrice?: GqlFormattedValue;
  netIncomeToCommon?: GqlFormattedValue;
  netSharePurchaseActivity?: unknown;
  nextFiscalYearEnd?: GqlFormattedValue;
  numberOfAnalystOpinions?: GqlFormattedValue;
  open?: GqlFormattedValue;
  operatingCashflow?: GqlFormattedValue;
  operatingMargins?: GqlFormattedValue;
  overallRisk?: number;
  payoutRatio?: GqlFormattedValue;
  phone?: string;
  postMarketChange?: GqlFormattedValue;
  postMarketChangePercent?: GqlFormattedValue;
  postMarketPrice?: GqlFormattedValue;
  postMarketTime?: number;
  preMarketChange?: GqlFormattedValue;
  preMarketChangePercent?: GqlFormattedValue;
  preMarketPrice?: GqlFormattedValue;
  preMarketTime?: number;
  previousClose?: GqlFormattedValue;
  priceHint?: GqlFormattedValue;
  priceToBook?: GqlFormattedValue;
  profitMargins?: GqlFormattedValue;
  quickRatio?: GqlFormattedValue;
  quoteType?: string;
  recommendationKey?: string;
  recommendationMean?: GqlFormattedValue;
  recommendationTrend?: unknown;
  regularMarketChange?: GqlFormattedValue;
  regularMarketChangePercent?: GqlFormattedValue;
  regularMarketDayHigh?: GqlFormattedValue;
  regularMarketDayLow?: GqlFormattedValue;
  regularMarketOpen?: GqlFormattedValue;
  regularMarketPreviousClose?: GqlFormattedValue;
  regularMarketPrice?: GqlFormattedValue;
  regularMarketTime?: number;
  regularMarketVolume?: GqlFormattedValue;
  returnOnAssets?: GqlFormattedValue;
  returnOnEquity?: GqlFormattedValue;
  revenueGrowth?: GqlFormattedValue;
  revenuePerShare?: GqlFormattedValue;
  sandP52WeekChange?: GqlFormattedValue;
  secFilings?: unknown;
  sector?: string;
  sectorDisp?: string;
  sectorKey?: string;
  sectorTrend?: unknown;
  shareholderRightsRisk?: number;
  sharesOutstanding?: GqlFormattedValue;
  sharesPercentSharesOut?: GqlFormattedValue;
  sharesShort?: GqlFormattedValue;
  sharesShortPriorMonth?: GqlFormattedValue;
  shortName?: string;
  shortPercentOfFloat?: GqlFormattedValue;
  shortRatio?: GqlFormattedValue;
  state?: string;
  symbol?: string;
  targetHighPrice?: GqlFormattedValue;
  targetLowPrice?: GqlFormattedValue;
  targetMeanPrice?: GqlFormattedValue;
  targetMedianPrice?: GqlFormattedValue;
  timeZoneFullName?: string;
  timeZoneShortName?: string;
  toCurrency?: string;
  topHoldings?: unknown;
  totalAssets?: GqlFormattedValue;
  totalDebt?: GqlFormattedValue;
  totalRevenue?: GqlFormattedValue;
  tradeable?: boolean;
  trailingAnnualDividendRate?: GqlFormattedValue;
  trailingAnnualDividendYield?: GqlFormattedValue;
  trailingEps?: GqlFormattedValue;
  trailingPe?: GqlFormattedValue;
  twoHundredDayAverage?: GqlFormattedValue;
  underlyingSymbol?: string;
  upgradeDowngradeHistory?: unknown;
  volume?: GqlFormattedValue;
  website?: string;
  week52Change?: GqlFormattedValue;
  yieldValue?: GqlFormattedValue;
  zip?: string;
}

/** Quote type metadata for a symbol (exchange, timezone, identifiers). */
export interface GqlQuoteTypeData {
  exchange?: string;
  firstTradeDateEpochUtc?: number;
  gmtOffSetMilliseconds?: number;
  longName?: string;
  maxAge?: number;
  messageBoardId?: string;
  quoteType?: string;
  shortName?: string;
  symbol?: string;
  timeZoneFullName?: string;
  timeZoneShortName?: string;
  underlyingSymbol?: string;
  uuid?: string;
}

/** Result of the batch `quotes` root field: successfully fetched quotes plus any per-symbol fetch errors. */
export interface GqlQuotesBatch {
  errors: GqlBatchError[];
  /** Successfully fetched quotes. */
  quotes: Page_GqlQuote;
}

/** Mirrors `finance_query::RatingConsensus`. */
export interface GqlRatingConsensus {
  buy?: number;
  consensus?: string;
  hold?: number;
  sell?: number;
  strongBuy?: number;
  strongSell?: number;
  symbol?: string;
}

/** Similar-stock recommendations for one symbol, mirroring `finance_query::Recommendation`, which has no serde rename of its own (plain snake_case JSON keys, e.g. `provider_id`) — `alias` keeps deserialization accepting those keys while `rename_all` documents the camelCase names GraphQL actually puts on the wire. */
export interface GqlRecommendation {
  providerId?: string;
  recommendations: GqlSimilarSymbol[];
  symbol: string;
}

/** Result of the batch `recommendationsBatch` root field: successfully fetched recommendations plus any per-symbol fetch errors. */
export interface GqlRecommendationsBatch {
  errors: GqlBatchError[];
  /** Successfully fetched per-symbol recommendations. */
  recommendations: Page_GqlRecommendation;
}

/** A research report result from `search`. */
export interface GqlResearchReport {
  author?: string;
  id?: string;
  provider?: string;
  reportDate?: number;
  reportHeadline?: string;
}

/** Mirrors `finance_query::RiskFactor`. */
export interface GqlRiskFactor {
  category?: string;
  filingDate?: string;
  text?: string;
  title?: string;
}

/** Risk/performance summary for a symbol. */
export interface GqlRiskSummary {
  beta?: number;
  calmar?: number;
  cvar95: number;
  cvar99: number;
  informationRatio?: number;
  kelly: number;
  maxDrawdown: number;
  maxDrawdownRecoveryPeriods?: number;
  omega: number;
  parametricCvar95: number;
  parametricVar95: number;
  sharpe?: number;
  sortino?: number;
  trackingError?: number;
  ulcerIndex: number;
  var95: number;
  var99: number;
}

/** A single quote result from a predefined or custom screener. */
export interface GqlScreenerQuote {
  ask?: GqlFormattedValue;
  askSize?: GqlFormattedValue;
  averageAnalystRating?: string;
  averageDailyVolume10Day?: GqlFormattedValue;
  averageDailyVolume3Month?: GqlFormattedValue;
  bid?: GqlFormattedValue;
  bidSize?: GqlFormattedValue;
  bookValue?: GqlFormattedValue;
  currency?: string;
  displayName?: string;
  dividendDate?: GqlFormattedValue;
  dividendRate?: GqlFormattedValue;
  dividendYield?: GqlFormattedValue;
  earningsTimestamp?: GqlFormattedValue;
  earningsTimestampEnd?: GqlFormattedValue;
  earningsTimestampStart?: GqlFormattedValue;
  epsCurrentYear?: GqlFormattedValue;
  epsForward?: GqlFormattedValue;
  epsTrailingTwelveMonths?: GqlFormattedValue;
  exchange: string;
  fiftyDayAverage?: GqlFormattedValue;
  fiftyDayAverageChange?: GqlFormattedValue;
  fiftyDayAverageChangePercent?: GqlFormattedValue;
  fiftyTwoWeekChange?: GqlFormattedValue;
  fiftyTwoWeekChangePercent?: GqlFormattedValue;
  fiftyTwoWeekHigh?: GqlFormattedValue;
  fiftyTwoWeekLow?: GqlFormattedValue;
  forwardPE?: GqlFormattedValue;
  longName?: string;
  marketCap?: GqlFormattedValue;
  postMarketChange?: GqlFormattedValue;
  postMarketChangePercent?: GqlFormattedValue;
  postMarketPrice?: GqlFormattedValue;
  postMarketTime?: GqlFormattedValue;
  preMarketChange?: GqlFormattedValue;
  preMarketChangePercent?: GqlFormattedValue;
  preMarketPrice?: GqlFormattedValue;
  preMarketTime?: GqlFormattedValue;
  priceEpsCurrentYear?: GqlFormattedValue;
  priceToBook?: GqlFormattedValue;
  quoteType: string;
  regularMarketChange: GqlFormattedValue;
  regularMarketChangePercent: GqlFormattedValue;
  regularMarketDayHigh?: GqlFormattedValue;
  regularMarketDayLow?: GqlFormattedValue;
  regularMarketOpen?: GqlFormattedValue;
  regularMarketPreviousClose?: GqlFormattedValue;
  regularMarketPrice: GqlFormattedValue;
  regularMarketTime?: GqlFormattedValue;
  regularMarketVolume?: GqlFormattedValue;
  sharesOutstanding?: GqlFormattedValue;
  shortName: string;
  symbol: string;
  trailingAnnualDividendRate?: GqlFormattedValue;
  trailingAnnualDividendYield?: GqlFormattedValue;
  trailingPE?: GqlFormattedValue;
  twoHundredDayAverage?: GqlFormattedValue;
  twoHundredDayAverageChange?: GqlFormattedValue;
  twoHundredDayAverageChangePercent?: GqlFormattedValue;
}

/** Results from a predefined or custom screener. */
export interface GqlScreenerResults {
  description?: string;
  lastUpdated?: number;
  /** Present only for `customScreener` (predefined `screener` has no underlying offset support upstream, so no pagination metadata to report). */
  pageInfo?: GqlPageInfo;
  quotes: GqlScreenerQuote[];
  total?: number;
  type: string;
}

/** A news article result from `search`. */
export interface GqlSearchNews {
  link?: string;
  providerPublishTime?: number;
  publisher?: string;
  relatedTickers?: string[];
  /** Opaque nested thumbnail object (resolutions/urls) — not curated further. */
  thumbnail?: unknown;
  title?: string;
  type?: string;
  uuid?: string;
}

/** A quote/symbol result from `search`. */
export interface GqlSearchQuote {
  dispSecIndFlag?: boolean;
  exchDisp?: string;
  exchange?: string;
  industry?: string;
  industryDisp?: string;
  isYahooFinance?: boolean;
  logoUrl?: string;
  longName?: string;
  quoteType?: string;
  score?: number;
  sector?: string;
  sectorDisp?: string;
  shortName?: string;
  symbol: string;
  typeDisp?: string;
}

/** Combined search results: quotes, news, and research reports. */
export interface GqlSearchResults {
  count?: number;
  news: GqlSearchNews[];
  /** Quote/symbol results. */
  quotes: Page_GqlSearchQuote;
  researchReports: GqlResearchReport[];
  totalTime?: number;
}

export interface GqlSectorCompany {
  dayChangePercent?: GqlFormattedValue;
  lastPrice?: GqlFormattedValue;
  marketCap?: GqlFormattedValue;
  marketWeight?: GqlFormattedValue;
  name?: string;
  rating?: string;
  symbol: string;
  targetPrice?: GqlFormattedValue;
  ytdReturn?: GqlFormattedValue;
}

export interface GqlSectorData {
  benchmark?: GqlSectorPerformance;
  benchmarkName?: string;
  industries: GqlSectorIndustry[];
  key: string;
  name: string;
  overview?: GqlSectorOverview;
  performance?: GqlSectorPerformance;
  researchReports: GqlSectorResearchReport[];
  symbol?: string;
  topCompanies: GqlSectorCompany[];
  topEtfs: GqlSectorETF[];
  topMutualFunds: GqlSectorMutualFund[];
}

export interface GqlSectorETF {
  expenseRatio?: GqlFormattedValue;
  lastPrice?: GqlFormattedValue;
  name?: string;
  netAssets?: GqlFormattedValue;
  symbol: string;
  ytdReturn?: GqlFormattedValue;
}

export interface GqlSectorIndustry {
  dayChangePercent?: GqlFormattedValue;
  key?: string;
  marketWeight?: GqlFormattedValue;
  name: string;
  symbol?: string;
  ytdReturn?: GqlFormattedValue;
}

export interface GqlSectorMutualFund {
  expenseRatio?: GqlFormattedValue;
  lastPrice?: GqlFormattedValue;
  name?: string;
  netAssets?: GqlFormattedValue;
  symbol: string;
  ytdReturn?: GqlFormattedValue;
}

export interface GqlSectorOverview {
  companiesCount?: number;
  description?: string;
  employeeCount?: GqlFormattedValue;
  industriesCount?: number;
  marketCap?: GqlFormattedValue;
  marketWeight?: GqlFormattedValue;
}

export interface GqlSectorPerformance {
  dayChangePercent?: GqlFormattedValue;
  fiveYearChangePercent?: GqlFormattedValue;
  oneYearChangePercent?: GqlFormattedValue;
  threeYearChangePercent?: GqlFormattedValue;
  ytdChangePercent?: GqlFormattedValue;
}

export interface GqlSectorResearchReport {
  headline?: string;
  id: string;
  investmentRating?: string;
  provider?: string;
  reportDate?: string;
  reportTitle?: string;
  reportType?: string;
  targetPrice?: number;
  targetPriceStatus?: string;
}

export interface GqlSentence {
  end: number;
  start: number;
  text: string;
  words: GqlWord[];
}

/** Lexicon-based sentiment score for a news article's title. */
export interface GqlSentiment {
  confidence: number;
  label: string;
  score: number;
}

/** A single recommended/similar symbol with its similarity score. */
export interface GqlSimilarSymbol {
  score: number;
  symbol: string;
}

/** Lightweight sparkline data for a single symbol (close prices only). */
export interface GqlSpark {
  closes: number[];
  interval?: string;
  meta: GqlChartMeta;
  range?: string;
  symbol: string;
  timestamps: number[];
}

/** Result of the batch `spark` root field: successfully fetched sparklines plus any per-symbol fetch errors. */
export interface GqlSparkBatch {
  errors: GqlBatchError[];
  sparks: GqlSpark[];
}

export interface GqlSpeakerData {
  company?: string;
  name: string;
  role?: string;
}

export interface GqlSpeakerMapping {
  speaker: number;
  speakerData: GqlSpeakerData;
}

/** A single stock split. */
export interface GqlSplit {
  denominator: number;
  numerator: number;
  ratio: string;
  timestamp: number;
}

/** Result of the batch `splitsBatch` root field: successfully fetched split histories plus any per-symbol fetch errors. */
export interface GqlSplitsBatch {
  errors: GqlBatchError[];
  splits: GqlSymbolSplits[];
}

/** Mirrors `finance_query::indicators::summary::StochasticData`, whose `k`/`d` fields serialize as `"%K"`/`"%D"` (a custom per-field rename, not plain camelCase) — must match exactly for deserialization to find them. */
export interface GqlStochasticData {
  d?: number;
  k?: number;
}

export interface GqlSuperTrendData {
  trend?: string;
  value?: number;
}

/** Wrapper for batch capital gains: `{symbol, capitalGains}`. */
export interface GqlSymbolCapitalGains {
  capitalGains: GqlCapitalGain[];
  symbol: string;
}

/** Wraps a symbol name with its chart data, used by the batch `charts` root field. */
export interface GqlSymbolChart {
  chart: GqlChart;
  symbol: string;
}

/** Wrapper for batch dividends: `{symbol, dividends}`. `dividends` is a plain payment list here (mirrors `BatchDividendsResponse.dividends: HashMap<String, Vec<Dividend>>`) — batch dividends has no per-symbol analytics, unlike the single-symbol `GqlDividends`. */
export interface GqlSymbolDividends {
  dividends: GqlDividend[];
  symbol: string;
}

/** Wrapper for batch financials: `{symbol, statement}` — `statement` is every line item in that symbol's statement, not a single one. */
export interface GqlSymbolFinancials {
  statement: GqlFinancialLineItem[];
  symbol: string;
}

/** Wraps a symbol name with its indicators, used by the batch root field. */
export interface GqlSymbolIndicators {
  indicators: GqlIndicatorsSummary;
  symbol: string;
}

/** Mirrors `finance_query::crypto::SymbolMatch` — the provider-neutral discovery shape, here populated from CoinGecko's coin universe. */
export interface GqlSymbolMatch {
  active?: boolean;
  assetType?: string;
  currency?: string;
  exchange?: string;
  id?: string;
  image?: string;
  marketCapRank?: number;
  name?: string;
  symbol: string;
  thumbnail?: string;
}

/** Wraps a symbol name with its options chain, used by the batch `optionsBatch` root field. */
export interface GqlSymbolOptions {
  options: GqlOptions;
  symbol: string;
}

/** Wrapper for batch splits: `{symbol, splits}`. */
export interface GqlSymbolSplits {
  splits: GqlSplit[];
  symbol: string;
}

export interface GqlTranscript {
  transcriptContent: GqlTranscriptContent;
  transcriptMetadata: GqlTranscriptMetadata;
}

export interface GqlTranscriptContent {
  companyId: number;
  eventId: number;
  speakerMapping: GqlSpeakerMapping[];
  transcript?: GqlTranscriptData;
  version?: string;
}

export interface GqlTranscriptData {
  numberOfSpeakers: number;
  /** Per-paragraph breakdown (speaker, start/end timestamp, text). A full call's `text` blob can be tens of thousands of tokens — paginate through paragraphs instead when only part of the call is needed. */
  paragraphs: Page_GqlParagraph;
  text: string;
}

export interface GqlTranscriptMetadata {
  date: number;
  eventId: number;
  eventType: string;
  fiscalPeriod: string;
  fiscalYear: number;
  isLatest: boolean;
  s3Url: string;
  title: string;
  transcriptId: number;
  transcriptType: string;
  updated: number;
}

export interface GqlTranscriptWithMeta {
  eventId: string;
  quarter?: string;
  title: string;
  transcript: GqlTranscript;
  url: string;
  year?: number;
}

/** One day of US Treasury yield curve rates. Maturities with no published rate on a given date are `null`. */
export interface GqlTreasuryYield {
  date: string;
  y1?: number;
  y10?: number;
  y1M?: number;
  y2?: number;
  y20?: number;
  y2M?: number;
  y3?: number;
  y30?: number;
  y3M?: number;
  y4M?: number;
  y5?: number;
  y6M?: number;
  y7?: number;
}

/** Mirrors `finance_query::crypto::TrendingCoin` — plain snake_case JSON keys, same no-serde-rename rule as [`GqlCoinQuote`]. */
export interface GqlTrendingCoin {
  id?: string;
  marketCapRank?: number;
  name?: string;
  priceBtc?: number;
  score?: number;
  symbol?: string;
}

/** A trending ticker. */
export interface GqlTrendingQuote {
  regularMarketChangePercent?: GqlFormattedValue;
  regularMarketPrice?: GqlFormattedValue;
  shortName?: string;
  symbol?: string;
}

export interface GqlWord {
  confidence: number;
  end: number;
  punctuatedWord: string;
  start: number;
  word: string;
}

/** Mirrors `finance_query::indicators::ZigZagPoint`, which has no serde rename of its own (plain snake_case keys). */
export interface GqlZigZagPoint {
  index?: number;
  isHigh?: boolean;
  price?: number;
}

/** Body of `GET /v2/health`. */
export interface HealthResponse {
  /** Data-provider attribution notices. */
  notices: string[];
  /** Always "healthy" when the server can respond at all. */
  status: string;
  /** RFC 3339 timestamp of the response. */
  timestamp: string;
  /** Server crate version. */
  version: string;
}

export interface Page_GqlCandle {
  edges: Record<string, unknown>[];
  nodes: GqlCandle[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlCotObservation {
  edges: Record<string, unknown>[];
  nodes: GqlCotObservation[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlDividend {
  edges: Record<string, unknown>[];
  nodes: GqlDividend[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlEdgarFiling {
  edges: Record<string, unknown>[];
  nodes: GqlEdgarFiling[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlFactDataPoint {
  edges: Record<string, unknown>[];
  nodes: GqlFactDataPoint[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlMacroObservation {
  edges: Record<string, unknown>[];
  nodes: GqlMacroObservation[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlOptionContract {
  edges: Record<string, unknown>[];
  nodes: GqlOptionContract[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlParagraph {
  edges: Record<string, unknown>[];
  nodes: GqlParagraph[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlQuote {
  edges: Record<string, unknown>[];
  nodes: GqlQuote[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlRecommendation {
  edges: Record<string, unknown>[];
  nodes: GqlRecommendation[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlSearchQuote {
  edges: Record<string, unknown>[];
  nodes: GqlSearchQuote[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlSymbolChart {
  edges: Record<string, unknown>[];
  nodes: GqlSymbolChart[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlSymbolDividends {
  edges: Record<string, unknown>[];
  nodes: GqlSymbolDividends[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlSymbolIndicators {
  edges: Record<string, unknown>[];
  nodes: GqlSymbolIndicators[];
  pageInfo: Record<string, unknown>;
}

export interface Page_GqlSymbolOptions {
  edges: Record<string, unknown>[];
  nodes: GqlSymbolOptions[];
  pageInfo: Record<string, unknown>;
}

/** Body of `GET /v2/ping`. */
export interface PingResponse {
  /** Always "pong". */
  message: string;
}

export type AnalysisResponse = unknown;

export type AnalysisType = "recommendations" | "upgrades-downgrades" | "earnings-estimate" | "earnings-history";

export type FilingSectionFormParam = "ten-k" | "eight-k";

export type Frequency = "annual" | "quarterly";

export type GqlEventKind = Record<string, unknown> | Record<string, unknown> | Record<string, unknown> | Record<string, unknown> | Record<string, unknown> | Record<string, unknown>;

export type GqlFormattedValue = number | number | string | GqlFormattedObject | unknown;

export type GqlMarketCalendarDetail = Record<string, unknown> | Record<string, unknown> | Record<string, unknown> | Record<string, unknown> | Record<string, unknown> | Record<string, unknown> | Record<string, unknown>;

export type HolderType = "major" | "institutional" | "mutualfund" | "insider-transactions" | "insider-purchases" | "insider-roster";

export type HoldersResponse = unknown;

export type Industry = "agricultural-inputs" | "aluminum" | "coal" | "copper" | "farm-products" | "forest-products" | "gold" | "lumber-wood-production" | "other-industrial-metals-mining" | "other-precious-metals-mining" | "silver" | "steel" | "thermal-coal" | "uranium" | "apparel-manufacturing" | "apparel-retail" | "auto-truck-dealerships" | "auto-manufacturers" | "auto-parts" | "beverages-brewers" | "beverages-non-alcoholic" | "beverages-wineries-distilleries" | "confectioners" | "department-stores" | "discount-stores" | "electronic-gaming-multimedia" | "food-distribution" | "footwear-accessories" | "furnishings-fixtures-appliances" | "gambling" | "grocery-stores" | "home-improvement-retail" | "household-personal-products" | "internet-retail" | "leisure" | "lodging" | "luxury-goods" | "packaged-foods" | "personal-services" | "residential-construction" | "resorts-casinos" | "restaurants" | "specialty-retail" | "textile-manufacturing" | "tobacco" | "travel-services" | "oil-gas-drilling" | "oil-gas-ep" | "oil-gas-equipment-services" | "oil-gas-integrated" | "oil-gas-midstream" | "oil-gas-refining-marketing" | "solar" | "asset-management" | "banks-diversified" | "banks-regional" | "capital-markets" | "credit-services" | "financial-data-stock-exchanges" | "insurance-brokers" | "insurance-diversified" | "insurance-life" | "insurance-property-casualty" | "insurance-reinsurance" | "insurance-specialty" | "mortgage-finance" | "shell-companies" | "biotechnology" | "diagnostics-research" | "drug-manufacturers-general" | "drug-manufacturers-specialty-generic" | "health-information-services" | "healthcare-plans" | "medical-care-facilities" | "medical-devices" | "medical-distribution" | "medical-instruments-supplies" | "pharmaceutical-retailers" | "aerospace-defense" | "building-materials" | "building-products-equipment" | "business-equipment-supplies" | "chemical-manufacturing" | "chemicals" | "conglomerates" | "consulting-services" | "electrical-equipment-parts" | "engineering-construction" | "farm-heavy-construction-machinery" | "industrial-distribution" | "infrastructure-operations" | "integrated-freight-logistics" | "manufacturing-diversified" | "marine-ports-services" | "marine-shipping" | "metal-fabrication" | "paper-paper-products" | "pollution-treatment-controls" | "railroads" | "rental-leasing-services" | "security-protection-services" | "specialty-business-services" | "specialty-chemicals" | "specialty-industrial-machinery" | "staffing-employment-services" | "tools-accessories" | "trucking" | "waste-management" | "real-estate-development" | "real-estate-diversified" | "real-estate-services" | "reit-diversified" | "reit-healthcare-facilities" | "reit-hotel-motel" | "reit-industrial" | "reit-mortgage" | "reit-office" | "reit-residential" | "reit-retail" | "reit-specialty" | "communication-equipment" | "computer-hardware" | "consumer-electronics" | "data-analytics" | "electronic-components" | "electronics-computer-distribution" | "hardware-software-distribution" | "information-technology-services" | "internet-content-information" | "scientific-technical-instruments" | "semiconductor-equipment-materials" | "semiconductors" | "software-application" | "software-infrastructure" | "broadcasting" | "entertainment" | "publishing" | "telecom-services" | "utilities-diversified" | "utilities-independent-power-producers" | "utilities-regulated-electric" | "utilities-regulated-gas" | "utilities-regulated-water" | "utilities-renewable" | "closed-end-fund-debt" | "closed-end-fund-equity" | "closed-end-fund-foreign" | "exchange-traded-fund";

export type Interval = "1m" | "2m" | "5m" | "15m" | "30m" | "1h" | "90m" | "1d" | "5d" | "1wk" | "1mo" | "3mo";

export type LookupType = "all" | "equity" | "mutualfund" | "etf" | "index" | "future" | "currency" | "cryptocurrency";

export type MarketCalendarKindParam = "earnings" | "ipo" | "dividend" | "split" | "economic" | "market-holiday" | "market-status";

export type MetricsText = string;

export type Quarter = "Q1" | "Q2" | "Q3" | "Q4";

export type QuoteType = "EQUITY" | "MUTUALFUND";

export type Region = "AR" | "AU" | "BR" | "CA" | "CN" | "DK" | "FI" | "FR" | "DE" | "GR" | "HK" | "IN" | "IL" | "IT" | "JP" | "KR" | "MY" | "MX" | "NZ" | "NO" | "PT" | "QA" | "RU" | "SG" | "ES" | "SE" | "TW" | "TH" | "TR" | "GB" | "US" | "VN";

export type Screener = "aggressive-small-caps" | "day-gainers" | "day-losers" | "growth-technology-stocks" | "most-actives" | "most-shorted-stocks" | "small-cap-gainers" | "undervalued-growth-stocks" | "undervalued-large-caps" | "conservative-foreign-funds" | "high-yield-bond" | "portfolio-anchors" | "solid-large-growth-funds" | "solid-midcap-growth-funds" | "top-mutual-funds";

export type Sector = "technology" | "financial-services" | "consumer-cyclical" | "communication-services" | "healthcare" | "industrials" | "consumer-defensive" | "energy" | "basic-materials" | "real-estate" | "utilities";

export type SortType = "ASC" | "DESC";

export type StatementType = "income" | "balance" | "cashflow";

export type TimeRange = "1d" | "5d" | "1mo" | "3mo" | "6mo" | "1y" | "2y" | "5y" | "10y" | "ytd" | "max";

export type ValueFormat = "raw" | "pretty" | "both";

export type indices_Region = "americas" | "europe" | "asia-pacific" | "middle-east-africa" | "currencies";
